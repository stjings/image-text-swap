# 이미지 텍스트 교체 툴 — 기획서 (PoC)

> 투명 PNG 타이틀 이미지의 글자를 추출·수정해 새 문구로 교체하고, 미리보기 후
> 다운로드하는 **서버 없는 정적 웹 앱**. GitHub Pages 배포, 100% 브라우저 처리.

**버전 v0.2** — v0.1 대비 변경점은 [부록 A](#부록-a-v01--v02-변경-요약) 참고.

---

## 1. 프로젝트 개요

이벤트 랜딩 등에 쓰이는 **투명 배경 PNG 타이틀 이미지**는 문구 하나 바꾸려 해도
디자인 원본(PSD/AI)을 열어 다시 export해야 한다. 이 툴은 **PNG 파일만 가지고**
글자를 읽어 들여 인풋박스에서 수정하고, 원래 색·위치·폰트 느낌을 유지한 채
새 문구로 합성해 돌려준다.

- **처리 방식**: B안 — 디자인 원본 없이 PNG 픽셀만으로 처리
- **배포**: GitHub Pages 정적 호스팅 (백엔드 없음)
- **연산 위치**: 전부 브라우저(WASM/Canvas). 이미지가 외부로 나가지 않아 보안상 유리
- **개발 도구**: Claude Code로 바닐라(HTML/CSS/JS) 구현

### 이 스코프를 택한 핵심 이유

일반적인 이미지 텍스트 교체에서 가장 어려운 단계는 **글자를 지운 자리의 배경 복원
(인페인팅)** 이다. 배경이 투명하면 이 단계가 `alpha = 0` 한 줄로 사라진다.
투명 PNG 한정이라는 제약이 곧 이 PoC의 성립 근거다. 불투명 배경을 비목표로 둔 것도
같은 맥락이며, 이 전제가 흔들리면 프로젝트 난이도가 한 단계 올라간다.

## 2. 목표 / 비목표 (Scope)

### 이번 버전에서 하는 것
- 투명 배경 PNG 한 장 업로드
- 이미지 내 텍스트 **자동 추출(OCR)** 후 인풋박스에 표시
- 인풋박스에서 문구 수정 → 저장 → 전체 반영 → 미리보기 → 다운로드
- 원본의 글자 **색(그라데이션 포함)·위치·크기** 재현
- 폰트 처리 모드 **사용자 선택**: 웹폰트 근사 선택 / 자동판별 위임

### 이번 버전에서 하지 않는 것 (비목표)
- 불투명·복잡한 배경 이미지 (배경 인페인팅이 필요 → 다음 단계)
- 글자에 입혀진 외곽선·그림자·패턴의 완벽 분리
- 다국어 OCR 튜닝(우선 한국어+영문/숫자)
- 서버 저장, 로그인, 이력관리 (실험 단계라 불필요)
- 가로 그라데이션·방사형 그라데이션 (세로 그라데이션만 대응)
- 블록 수동 병합/분리 UI (오검출 시 인풋박스에서 문구만 수정)

## 3. 대상 사용자
- **1차**: 사내 사업부 — 타이틀 문구만 셀프로 바꿔 쓰고 싶은 비개발 담당자
- **2차(실험)**: 같은 니즈를 가진 외부 사용자 — 실험적 서드파티 앱으로 공개 운영 검토

## 4. 핵심 사용자 플로우

```
[1] PNG 업로드 (드래그&드롭 / 파일선택)
        │
        ▼
[2] 자동 분석
     - 알파 채널로 글자 영역 검출 (bbox)
     - 알파 마스크를 OCR용 흑백 이미지로 전처리 (7-0)
     - Tesseract.js로 텍스트 OCR 추출
     - 영역별로 인풋박스 자동 생성 + 추출된 문구 채워넣기
        │
        ▼
[3] 사용자 편집
     - 각 인풋박스에서 문구 수정
     - [저장] 으로 개별 확정 (편집 완료 표시)
        │
        ▼
[4] 폰트 모드 선택
     - (A) 직접 선택: 웹폰트 후보 목록에서 고름
     - (B) 자동판별: 원본 글자와 가장 닮은 후보를 시스템이 선택
        │
        ▼
[5] [수정 요청] 클릭 → 합성
     - 원본 글자 제거(알파=0)
     - 추출 색·위치·크기·(선택)폰트로 새 문구 렌더 후 합성
     - 길이 초과 시 7-6 오버플로 규칙 적용
        │
        ▼
[6] 미리보기 (체커보드 위, 원본/결과 토글 비교)
        │
        ▼
[7] 다운로드 (투명 PNG로 저장)
```

## 5. 화면 구성 (단일 페이지)

```
┌───────────────────────────────────────────────┐
│  이미지 텍스트 교체 툴                            │
├───────────────────────────────────────────────┤
│  ┌─────────── 업로드 영역 ───────────┐           │
│  │  PNG를 끌어다 놓거나 [파일 선택]     │           │
│  └──────────────────────────────────┘           │
│                                                 │
│  [ 원본 미리보기 (체커보드 배경) ]                 │
│                                                 │
│  추출된 텍스트                                    │
│   ┌──────────────────────────┐ [저장] ✓         │
│   │ 합격을 응원합니다            │                 │
│   └──────────────────────────┘                 │
│   ( 텍스트 블록이 여러 개면 인풋박스도 여러 개 )     │
│                                                 │
│  폰트 처리  ( ) 직접 선택 [Pretendard ▼]          │
│            (•) 자동판별에 맡기기                   │
│                                                 │
│            [ 전체 저장 ]   [ 수정 요청 ]           │
├───────────────────────────────────────────────┤
│  결과 미리보기   [원본] [결과] 토글                │
│   [ 합성 결과 (체커보드 배경) ]                    │
│   ⚠ 문구가 길어 90%로 축소했습니다 (해당 시)       │
│            [ PNG 다운로드 ]                       │
└───────────────────────────────────────────────┘
```

## 6. 기술 스택

| 역할 | 선택 | 비고 |
|------|------|------|
| 호스팅 | GitHub Pages | 정적, 무료 |
| 마크업/로직 | Vanilla HTML/CSS/JS | 프레임워크 불필요, 의존성 최소 |
| OCR | **Tesseract.js** | 한국어 `kor` + `eng` traineddata, word/line bbox 제공 |
| 이미지 처리 | **Canvas 2D API** | 알파 조작·색 추출·글자 렌더·합성 전부 담당 |
| 폰트 | 웹폰트(woff2) 임베드 | 브랜드 폰트/공개 웹폰트 후보군 등록 |
| 다운로드 | `canvas.toBlob()` → `a[download]` | 투명 PNG 저장 |

> 앞서 파이썬 Pillow로 검증한 파이프라인(검출→색추출→제거→합성→폰트판별)을
> 브라우저 Canvas로 1:1 이식하는 구조. 알고리즘은 이미 PoC로 입증됨.

### 6-1. Tesseract.js 자산 배치 (v0.2 추가)

Tesseract.js는 CDN 스크립트 외에 **worker 스크립트·wasm 코어·traineddata** 세
가지를 별도로 받는다. GitHub Pages 환경에서 경로/CORS 문제가 생기기 쉬우므로
초기화 시 세 경로를 명시적으로 지정한다.

- 1차: CDN(jsDelivr/unpkg) 사용 — 구현이 빠름
- 폴백: `vendor/tesseract/` 에 worker·wasm·`kor.traineddata`·`eng.traineddata`를
  저장소에 동봉하고 상대경로로 지정. 사내망 등 외부 CDN이 막힌 환경 대비.

`kor.traineddata`는 압축본 기준으로도 십수 MB 규모다. **최초 1회 로딩 진행률 UI가
필수**이며, 한 번 받은 뒤에는 브라우저 캐시가 처리한다.

## 7. 처리 파이프라인 (핵심 로직)

### 7-0. OCR 전처리 — 알파 마스크 변환 (v0.2 추가, 필수)

투명 PNG를 캔버스에서 그대로 읽으면 투명 픽셀이 `RGBA(0,0,0,0)`, 즉 **검정**으로
취급된다. 흰색 글자 타이틀이라면 OCR 입장에서 "검정 배경 위 검정 글자"가 되어
인식률이 0에 수렴한다. 따라서 OCR에 넣기 전 알파 채널만으로 흑백 이미지를 만든다.

```
글자 픽셀(alpha 높음) → 검정(0,0,0)
배경 픽셀(alpha 낮음) → 흰색(255,255,255)
즉  gray = 255 - alpha,  출력 알파는 255로 고정
```

부수 효과로 그라데이션·장식 색상이 전부 날아가 **11장의 "그라데이션에서 오탈자"
리스크를 상당 부분 함께 해소**한다. 글자 높이가 작으면(대략 30px 미만) 2배
업스케일 후 OCR에 넣는다.

### 7-1. 글자 영역 검출
- `getImageData`로 알파 채널 추출 → `alpha > 임계값(20)` 픽셀이 글자
- 가로/세로 투영(projection profile)으로 줄·덩어리 1차 분리
- OCR line/word bbox와 교차시켜 **텍스트 블록 단위**로 영역 확정
- 블록마다 인풋박스 1개 매핑
- 블록 bbox는 **실제 글자 픽셀의 잉크 bbox**로 확정 (OCR bbox는 여유 패딩이 있어
  그대로 쓰면 폰트 크기 역산이 어긋난다)

### 7-2. 색 추출 (그라데이션 대응)
- 각 블록 글자 픽셀 중 상단 25% / 하단 25%의 평균색 → 세로 그라데이션 양 끝 추정
- 단색이면 top≈bottom으로 자연 수렴 (분기 없이 한 경로로 처리)
- 평균 계산 시 **알파 가중 평균**을 쓴다. 안티에일리어싱된 반투명 외곽 픽셀을
  동일 가중치로 넣으면 색이 흐려진다

### 7-3. 글자 제거
- 해당 블록 영역의 **알파를 0으로** → 투명. **배경 복원(인페인팅) 불필요**
- (투명 PNG라 이 단계가 가장 쉬움 — 이 스코프를 택한 핵심 이유)

**bbox 사각형으로 지우지 않는다** (v0.2 수정). 사각형으로 밀면 bbox 안에 겹쳐 있는
장식 요소(밑줄, 도형, 아이콘)까지 함께 사라진다. 대신:

1. 블록 내에서 `alpha > 20`인 글자 픽셀 집합을 구한다
2. 이 집합을 2~3px **dilate**(팽창)해 제거 마스크를 만든다
3. 마스크 안의 픽셀만 `alpha = 0`

dilate가 필요한 이유는 안티에일리어싱 때문이다. 검출 임계값(20) 이하의 반투명
외곽 픽셀을 남기면 글자 윤곽이 유령처럼 잔상으로 남는다.

### 7-4. 새 문구 합성

**폰트 사이즈는 공식으로 역산하지 않고 피팅 루프로 수렴시킨다** (v0.2 수정).
bbox 높이는 실제 글리프의 잉크 높이지만 `font-size`는 em 기준이라 폰트마다 비율이
다르고, 한글은 라틴 대비 글자가 박스를 꽉 채운다. 한 번의 곱셈으로는 안 맞는다.

```
size = blockHeight * 1.2            // 초기 추정
3회 반복:
  렌더 후 measureText()의
  actualBoundingBoxAscent + actualBoundingBoxDescent → 실제 잉크 높이 h'
  size *= blockHeight / h'          // 배율 보정
```

이후:
- 선택/판별된 폰트로 오프스크린 Canvas에 렌더
- 세로 그라데이션 색 입힘 (`createLinearGradient`, 잉크 bbox 상단→하단)
- 원위치 합성. **정렬 추정**: 기본은 원본 블록 bbox 중앙 정렬. 단 블록이 여러 개고
  좌측 x좌표가 서로 정렬돼 있으면 좌측정렬로 간주한다

### 7-5. 폰트 자동판별 (모드 B)

- 후보 웹폰트 각각으로 원문 글자를 렌더 → 이진 실루엣 생성
- 원본 글자 실루엣과 **IoU(겹침 비율)** 비교 → 최고 점수 폰트 선택

**IoU 계산 전 정규화가 필수다** (v0.2 수정). 두 실루엣을 그냥 겹치면 IoU가
"모양 유사도"가 아니라 "위치·크기 오차"를 측정하게 되어 점수가 무의미해진다.

```
1) 각 실루엣을 잉크 bbox로 크롭
2) 동일 높이로 리스케일
3) 무게중심(centroid) 정렬
4) 그 다음 IoU 계산
```

- 신뢰도 표기는 IoU 절대값 대신 **1위와 2위의 점수 격차**를 함께 본다. 격차가
  작으면 "판별: OO (후보 간 차이 작음)" 으로 낮은 확신을 드러낸다
- 후보군이 좁을수록 정확 (브랜드 폰트 5~6개 등록 권장)
- 이 로직은 **OCR 결과가 정확할 때만 의미가 있다.** 원문이 틀리면 실루엣 자체가
  틀린 글자라 판별이 무너진다 → M5는 M2 품질에 종속됨

### 7-6. 오버플로 규칙 (v0.2 신설)

새 문구가 원본보다 길거나 짧은 경우의 처리 규칙. **실사용에서 가장 자주 발생하는
케이스**이므로 명시적으로 정의한다.

문구가 원본 bbox 폭을 넘칠 때, 아래 순서로 적용하고 첫 단계에서 해결되면 멈춘다:

| 순서 | 조치 | 한도 |
|------|------|------|
| 1 | 자간(letter-spacing) 압축 | 최대 -5% |
| 2 | 폰트 크기 축소 | 원본의 70%까지 |
| 3 | bbox 가로 확장 (정렬 기준점 유지) | 캔버스 경계까지 |
| 4 | 경고 표시 후 그대로 렌더 | — |

- 2·3단계가 적용되면 결과 미리보기에 사유를 노출한다
  (예: "문구가 길어 90%로 축소했습니다")
- **문구가 짧아진 경우 폰트 크기를 키우지 않는다.** 원본 크기를 유지하고 정렬
  기준에 맞춰 재배치만 한다. 크기를 키우면 다른 블록과 시각적 위계가 깨진다

## 8. 폰트 처리 — 사용자 선택 UX

라디오로 두 모드 제공:

- **직접 선택**: 등록된 웹폰트 후보를 드롭다운으로 노출. 미리보기에 즉시 반영.
  아는 사용자가 정확히 지정.
- **자동판별에 맡기기(기본값)**: 7-5 로직으로 가장 닮은 폰트를 자동 선택.
  결과 옆에 "판별: OO (유사도 82%)" 를 표기해 신뢰도를 보여줌.

> 폰트가 정확히 안 맞아도 **교체 자체는 항상 성공**한다(PoC로 확인). 자동판별은
> "더 자연스럽게" 만드는 보조 장치이지 성패를 가르는 요소가 아님.

## 9. 데이터 모델 (v0.2 신설)

`app.js`가 들고 있을 상태의 형태. 모듈 간 계약을 먼저 고정해 두면 M2~M5를
독립적으로 붙일 수 있다.

```js
state = {
  sourceImage,          // ImageBitmap — 원본
  blocks: [
    {
      id,
      bbox: {x, y, w, h},     // 잉크 bbox (7-1)
      originalText,           // OCR 원문
      editedText,             // 사용자 수정본
      saved: false,           // [저장] 눌렀는지
      colorTop, colorBottom,  // 그라데이션 양 끝 (7-2)
      silhouette,             // 정규화된 이진 실루엣 (7-5용)
      align: 'center',        // 'center' | 'left'
      fittedSize,             // 피팅 루프 결과 (7-4)
      overflowNote: null      // 오버플로 조치 사유 (7-6)
    }
  ],
  fontMode: 'auto',     // 'auto' | 'manual'
  selectedFont,         // manual 모드일 때
  detectedFont: {name, score, runnerUpGap}   // auto 모드 결과
}
```

## 10. 프로젝트 구조 (Claude Code 생성 대상)

```
image-text-swap/            # 실제 저장소명 (v0.1의 image-text-tool 에서 수정)
├── index.html              # 단일 페이지 UI
├── PLAN.md                 # 이 문서
├── css/
│   └── style.css
├── js/
│   ├── app.js              # 플로우 오케스트레이션·상태관리
│   ├── ocr.js              # Tesseract.js 래퍼 (전처리·추출·bbox)
│   ├── detect.js           # 알파 검출·블록 분리·색 추출
│   ├── compose.js          # 제거·렌더·합성·오버플로·다운로드
│   └── fontmatch.js        # 폰트 후보 렌더·정규화·IoU 판별
├── fonts/                  # 웹폰트 후보 (woff2) — 브랜드 폰트 배치
│   └── fonts.css
├── vendor/                 # (폴백 시) tesseract worker·wasm·traineddata
├── assets/
│   └── sample.png          # 테스트용 투명 타이틀 샘플
└── README.md
```

## 11. 구현 마일스톤

각 마일스톤은 **완료 판정 기준**을 함께 둔다.

| # | 이름 | 내용 | 완료 판정 |
|---|------|------|-----------|
| **M0** | **스파이크** | 문구를 하드코딩해 "제거 → 재합성"만 수동 실행 | 샘플 이미지에서 결과가 원본과 비슷해 보임 |
| M1 | 뼈대 | 업로드 + 원본 미리보기(체커보드) + Canvas 로드 | PNG를 올리면 체커보드 위에 뜬다 |
| M2 | 추출 | 알파 마스크 전처리(7-0) + 블록 분리 + OCR → 인풋박스 자동 생성 | 샘플에서 문구가 대체로 읽히고 블록 수가 맞다 |
| M3 | 편집/저장 | 인풋 수정·개별 저장·전체 저장 상태관리 | 저장 표시가 정확히 동작한다 |
| M4 | 합성 | 색 추출 → 제거 → 피팅 루프 렌더·합성 → 결과 미리보기 + 오버플로 규칙 | 긴 문구·짧은 문구 모두 깨지지 않는다 |
| M5 | 폰트 | 직접 선택 드롭다운 + 자동판별(정규화 IoU) + 신뢰도 표기 | 후보에 정답 폰트를 넣으면 1위로 뽑힌다 |
| M6 | 마감 | 원본/결과 토글, PNG 다운로드, GitHub Pages 배포 | 배포 URL에서 전 과정이 동작한다 |

### M0을 M2보다 먼저 두는 이유 (v0.2 변경)

v0.1은 M1→M2(추출)를 먼저 확인하는 순서였다. 하지만 **이 프로젝트의 품질 상한선을
결정하는 것은 OCR이 아니라 합성 결과**다.

- OCR이 틀리면 → 인풋박스 수동 수정으로 덮인다 (설계된 안전망)
- 합성이 어색하면 → **덮을 방법이 없다**

따라서 문구를 하드코딩해서라도 "지우고 다시 그린 결과가 원본처럼 보이는가"를 반나절
안에 먼저 확인한다. 여기서 결과가 납득되지 않으면 M2 이후를 만들 이유가 없다.

## 12. 한계 · 리스크

- **브라우저 한글 OCR 정확도**: Tesseract.js `kor`는 장식 폰트/그라데이션에서
  오탈자 가능 → 그래서 **인풋박스 수동수정을 기본 내장**(이게 안전망).
  7-0 전처리로 상당 부분 완화되지만 완전 해결은 아니다
- **traineddata 용량**: `kor.traineddata` 로딩 시간 → 최초 1회 로딩 UX(진행률) 필요.
  CDN 차단 환경 대비 자체 호스팅 폴백(6-1)
- **폰트 라이선스**: 브랜드 폰트를 웹폰트로 임베드해 공개 배포 시 라이선스 확인 필수
  (사내 전용/외부 공개에 따라 허용 범위 다름 — 배포 전 검토).
  **외부 공개 시점 이전에 결론이 나야 하는 유일한 비기술 블로커**
- **효과 있는 글자**(외곽선·그림자): 이번 스코프 밖. 단색/그라데이션 우선.
  외곽선이 있으면 7-2 색 추출이 외곽선 색에 오염된다
- **여러 줄·복합 레이아웃**: 블록 분리가 틀릴 수 있음 → 이번 버전에서는 인풋박스
  문구 수정으로만 대응, 수동 병합/분리 UI는 다음 단계
- **폰트 로딩 타이밍**: 웹폰트가 로드되기 전에 캔버스에 렌더하면 폴백 폰트로 그려진다.
  합성·판별 전에 `document.fonts.ready` 대기 필수 (조용히 틀리는 종류의 버그)

## 13. 착수 전 확정 필요 사항 (v0.2 신설)

구현을 막지는 않지만, 없으면 검증 품질이 떨어지는 항목:

1. **테스트용 샘플 이미지** — `assets/sample.png`. 실제 사용할 타이틀 이미지 1~2장.
   M0 검증이 여기에 전적으로 달려 있다
2. **브랜드 웹폰트 후보 목록** — 5~6개. 없으면 공개 웹폰트(Pretendard, 나눔 계열 등)로
   시작하고 나중에 교체한다
3. **외부 공개 여부** — 폰트 라이선스 검토 범위가 달라진다. M6 배포 전까지 결정

## 14. 향후 확장
- 불투명 배경 대응 (배경 인페인팅 단계 추가)
- 외곽선/그림자 레이어 분리·재현
- 블록 수동 병합/분리 보정 UI
- 브랜드 폰트 사전 자동 등록·관리 화면
- 배치 처리(여러 이미지 일괄 교체)
- 회사 디자인 원본(피그마 API) 연동 A안과 하이브리드

## 15. Claude Code 착수 프롬프트 (예시)

```
투명 PNG 타이틀 이미지의 글자를 교체하는 정적 웹 앱을 만들어줘.
- 서버 없이 GitHub Pages에 배포되는 순수 HTML/CSS/JS (프레임워크 X)
- 이 기획서(PLAN.md)의 섹션 4 플로우와 섹션 10 구조를 그대로 따를 것
- OCR은 Tesseract.js(kor+eng), 이미지 처리는 Canvas 2D
- OCR 전에 섹션 7-0의 알파 마스크 전처리를 반드시 넣을 것
- 폰트는 '직접 선택' / '자동판별(정규화 IoU)' 두 모드, 기본값은 자동판별
- 먼저 M0(문구 하드코딩 상태로 제거→재합성)만 만들고 멈춰서 결과를 보여줘.
  합성 품질이 납득되면 그때 M1~M2로 넘어간다.
```

---

## 부록 A. v0.1 → v0.2 변경 요약

| # | 항목 | 변경 내용 | 이유 |
|---|------|-----------|------|
| 1 | **7-0 신설** | OCR 전 알파 마스크 → 흑백 변환 단계 추가 | 투명 픽셀이 검정으로 읽혀 흰 글자 OCR이 사실상 불가능. **미반영 시 M2가 통째로 실패** |
| 2 | 7-4 수정 | 폰트 크기 "역산" → 3회 피팅 루프 | 잉크 높이와 em 크기의 비율이 폰트·언어별로 달라 단일 공식으로 안 맞음 |
| 3 | **7-6 신설** | 오버플로 4단계 규칙 (자간→축소→확장→경고) | 문구 길이 변화는 실사용 최빈 케이스인데 v0.1에 규칙 부재 |
| 4 | 7-5 수정 | IoU 전 크롭·리스케일·무게중심 정렬 추가 | 정규화 없는 IoU는 모양이 아니라 위치 오차를 측정 |
| 5 | 7-3 수정 | bbox 사각형 제거 → 글자 픽셀 dilate 마스크 제거 | 사각형은 겹친 장식 요소까지 삭제. dilate는 안티에일리어싱 잔상 방지 |
| 6 | 7-1 보강 | OCR bbox 대신 잉크 bbox로 확정, 투영 기반 1차 분리 | OCR bbox의 패딩이 크기 역산을 어긋나게 함 |
| 7 | 7-2 보강 | 알파 가중 평균 | 반투명 외곽 픽셀 동일 가중 시 색이 흐려짐 |
| 8 | 6-1 신설 | Tesseract 자산 자체 호스팅 폴백 | CDN 차단 환경 및 worker/wasm 경로 이슈 대비 |
| 9 | **마일스톤** | M0(합성 스파이크)를 최우선으로 신설 | OCR 오류는 수동수정으로 덮이지만 합성 품질은 못 덮음. 상한선을 먼저 확인 |
| 10 | 9장 신설 | 상태 데이터 모델 명시 | 모듈 간 계약 고정으로 M2~M5 독립 구현 가능 |
| 11 | 12장 보강 | 웹폰트 로딩 타이밍(`document.fonts.ready`) 리스크 추가 | 조용히 폴백 폰트로 렌더되는 디버깅 난이도 높은 버그 |
| 12 | 13장 신설 | 착수 전 확정 필요 사항 3건 | 샘플 이미지 없이는 M0 검증 자체가 불가 |
| 13 | 구조 | `image-text-tool/` → `image-text-swap/` | 실제 저장소명과 불일치 |
| 14 | 2장 보강 | 가로/방사형 그라데이션, 블록 병합 UI를 비목표에 명시 | 암묵적이던 스코프 경계를 명문화 |
