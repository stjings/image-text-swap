# 이미지 텍스트 교체 툴 — 기획서 (PoC)

> 타이틀/이벤트 이미지의 글자를 추출·수정해 새 문구로 교체하고, 미리보기 후
> 다운로드하는 **서버 없는 정적 웹 앱**. GitHub Pages 배포, 100% 브라우저 처리.

**버전 v1.3** — 디자인팀 실사용 폰트(SUIT·Pretendard) 등록, 자동판별 후보 한정.
변경 이력은 [부록 A](#부록-a-변경-이력), M0 결과는 [부록 B](#부록-b-m0-스파이크-결과).

---

## 1. 프로젝트 개요

이벤트 랜딩 등에 쓰이는 **타이틀·이벤트 이미지**는 문구 하나 바꾸려 해도
디자인 원본(PSD/AI)을 열어 다시 export해야 한다. 이 툴은 **이미지 파일만 가지고**
글자를 읽어 들여 인풋박스에서 수정하고, 원래 색·위치·폰트 느낌을 유지한 채
새 문구로 합성해 돌려준다.

- **처리 방식**: B안 — 디자인 원본 없이 픽셀만으로 처리
- **배포**: GitHub Pages 정적 호스팅 (백엔드 없음)
- **연산 위치**: 전부 브라우저(WASM/Canvas). 이미지가 외부로 나가지 않아 보안상 유리
- **개발 도구**: Claude Code로 바닐라(HTML/CSS/JS) 구현

### 이 스코프를 택한 핵심 이유

이미지 텍스트 교체에서 가장 어려운 단계는 **글자를 지운 자리의 배경 복원**이다.
이 툴은 **배경 복원이 쉬운 경우로 대상을 한정**해 그 난제를 우회한다.

- 배경이 **투명**이면 → `alpha = 0` 한 줄
- 배경이 **단색**이면 → 주변 색을 샘플링해 칠하기

이 두 경우만 지원하고, 사진·그라데이션·패턴 위의 글자는 이번 버전에서 제외한다
(인페인팅 필요). 실제 샘플 검토 결과 **이벤트 이미지 텍스트의 대부분이 위 두
경우에 해당**해 실용 커버리지는 충분하다. 자세한 판정은 7-1 참고.

## 2. 목표 / 비목표 (Scope)

### 이번 버전에서 하는 것
- 이미지 한 장 업로드 (PNG 우선, JPG도 단색 배경 블록은 처리 가능)
- 이미지 내 텍스트 **자동 추출(OCR)** 후 블록별 인풋박스에 표시
- **블록별 배경 유형 자동 판정** — 편집 가능/불가를 사용자에게 명시
- 원하는 블록만 골라 수정 → 미리보기 → 다운로드
- 원본의 글자 **색(그라데이션 포함)·위치·크기·자간** 재현
- 폰트 처리 모드 **사용자 선택**: 웹폰트 근사 선택 / 자동판별 위임

### 이번 버전에서 하지 않는 것 (비목표)
- **사진·그라데이션·패턴 위에 얹힌 글자** (배경 인페인팅 필요 → 다음 단계)
- 글자에 입혀진 외곽선·그림자·패턴의 완벽 분리
- 다국어 OCR 튜닝(우선 한국어+영문/숫자)
- 서버 저장, 로그인, 이력관리 (실험 단계라 불필요)
- 가로·방사형 그라데이션 (세로 그라데이션만 대응)
- 블록 수동 병합/분리 UI (오검출 시 문구 수정으로만 대응)
- 이미지 리사이즈·레이아웃 재배치 (문구 교체만)

## 3. 대상 사용자
- **1차**: 사내 사업부 — 타이틀 문구만 셀프로 바꿔 쓰고 싶은 비개발 담당자
- **2차(실험)**: 같은 니즈를 가진 외부 사용자 — 실험적 서드파티 앱으로 공개 운영 검토

## 4. 기준 샘플 (v0.3 신설)

기획·구현·검증의 기준이 되는 실제 이미지 2종. 난이도가 크게 다르므로 각각
다른 역할을 맡는다.

아래 수치는 v0.4에서 **실제 파일을 측정한 값**이다(눈대중 추정 아님).

### 샘플 A — 단순형 (`assets/sample-simple.png`)

| 항목 | 실측값 |
|------|--------|
| 포맷 / 크기 | RGBA PNG, 1140 × 184 |
| 완전 투명 (α=0) | **84.45%** |
| 반투명 (0<α<255) | 4.08% |
| 완전 불투명 (α=255) | 11.47% |

**줄이 2개가 아니라 3개다** (M0 발견). 육안으로는 빨간 글씨 두 줄만 보이지만,
맨 위에 **흰색 반투명 줄이 하나 더 있다.**

| # | 문구 | 색 | 불투명도 | 폰트 판별 |
|---|------|-----|----------|-----------|
| 0 | 더 이상의 설명은 필요없습니다. | RGB(255,255,255) | **60%** | Noto Sans KR 400 |
| 1 | 2027 합격을 위한 행정법 | RGB(226,26,0) | 100% | Gothic A1 900 |
| 2 | 결국, 써니 뿐입니다. | RGB(226,26,0) | 100% | Black Han Sans 400 |

0번 줄은 흰 배경 위에서는 사실상 보이지 않는다. 사람 눈으로 검수하는 것과 툴이
검출하는 것이 다를 수 있다는 실례이며, **미리보기 배경을 체커보드로 깔아야 하는
직접적인 근거**다. 흰 배경 미리보기였다면 사용자는 이 블록이 왜 잡혔는지 알 수 없다.

세 줄 모두 배경이 완전 투명한 **유형 A**다.
**M0 스파이크 대상이자 통과 확인 완료** ([부록 B](#부록-b-m0-스파이크-결과)).

### 샘플 B — 복합형 (`assets/sample-complex.png`)

> 이벤트 랜딩 전체 그래픽 (헤드라인 + 제품 사진 + 정보 박스)

| 항목 | 실측값 |
|------|--------|
| 포맷 / 크기 | RGBA PNG, 1140 × 921 |
| 완전 투명 (α=0) | **48.02%** |
| 반투명 (0<α<255) | 10.40% — 그림자·소프트 엣지 |
| 완전 불투명 (α=255) | 41.58% |

**v0.3의 예상이 틀렸다.** 이 이미지는 불투명 그래픽이 아니라 **절반이 투명한
RGBA PNG**다. 상단 텍스트 영역이 흰 배경인 줄 알았으나 실제로는 전부 투명이었다.
결과적으로 v0.3에서 걱정한 것보다 조건이 훨씬 좋다.

**M4 이후의 실전 검증 대상.** 전부 되는 것이 목표가 아니라, **되는 블록과 안 되는
블록을 툴이 정확히 구분해 알려주는 것**이 합격 기준이다.

### 샘플 B 블록별 배경 유형 (7-1 알고리즘 실측 판정)

| 블록 | 배경 실측 | 유형 | 대표색 |
|------|-----------|------|--------|
| `EVENT 02.` | 완전 투명 | **A** | — |
| 인용문 2행 | 완전 투명 | **A** | — |
| 메인 헤드라인 2행 | 완전 투명 | **A** | — |
| 각주 (`*YES24 베스트셀러…`) | 완전 투명 | **A** | — |
| 파란 박스 내 텍스트 | 불투명 단색 | **B** | RGB(194~205, 221~227, 255) |
| `추첨 5명` 배지 | 불투명 단색(검정 원) | **B** | RGB(36, 35, 35) |
| `추첨 10명` 배지 | 불투명, 균일도 미달 | **C** | — (잠금) |
| 배경 워터마크 `EVENT` | 그라데이션 | — | 편집 대상 아님 |

**측정으로 확인된 사실**

- 텍스트가 사진 위에 직접 얹힌 경우는 **없다**. 원형 배지도 안쪽이 단색이다
- 각주 글자는 **불투명 픽셀이 0%** 다. 즉 회색이 아니라 **알파로 표현된 반투명 글자**이며,
  색 추출 시 알파 가중 평균(7-4)이 없으면 색이 틀어진다
- 파란 박스 배경은 완전 단색이 아니라 **위치에 따라 RGB가 10 남짓 변하는 미세
  그라데이션**이다. 대표색을 이미지 전역이 아니라 **블록마다 따로** 구해야 하는 근거
- `추첨 10명` 배지가 C로 떨어진 것은 원 주변 이미지가 표본에 섞였기 때문으로 보인다.
  **애매하면 잠그는 방향**이라 실패 방향이 안전하다 (잘못 칠하는 것보다 낫다)

## 5. 핵심 사용자 플로우

```
[1] 이미지 업로드 (드래그&드롭 / 파일선택)
        │
        ▼
[2] 자동 분석
     - 알파/명암으로 글자 픽셀 검출 → 블록 분리 (7-3)
     - 블록별 배경 유형 판정: A(투명) / B(단색) / C(복잡) (7-1)
     - OCR 전처리 후 Tesseract.js로 문구 추출 (7-2)
     - 블록 목록 생성. C유형은 잠금 표시
        │
        ▼
[3] 사용자 편집
     - 이미지 위 블록 오버레이에서 편집할 블록 클릭
     - 문구 수정 → [저장]
     - ★ 수정하지 않은 블록은 원본 픽셀 그대로 보존
        │
        ▼
[4] 폰트 모드 선택
     - (A) 직접 선택: 웹폰트 후보 목록에서 고름
     - (B) 자동판별: 원본 글자와 가장 닮은 후보를 시스템이 선택
        │
        ▼
[5] [수정 요청] 클릭 → 합성 (수정된 블록만)
     - 유형 A: 알파를 0으로 / 유형 B: 배경색으로 채우기
     - 추출 색·위치·크기·자간·(선택)폰트로 새 문구 렌더 후 합성
     - 길이 초과 시 7-8 오버플로 규칙 적용
        │
        ▼
[6] 미리보기 (원본/결과 토글 비교, 투명 영역은 체커보드)
        │
        ▼
[7] 다운로드 (PNG로 저장, 투명도 보존)
```

## 6. 화면 구성 (v1.1 재구성)

**조작은 위에 고정, 정보는 아래에 고정, 가운데는 작업 영역**으로 나눈다.
v1.0까지는 조작 버튼이 상단과 하단에 흩어져 있었고 이미지 정보가 미리보기 바로
아래 붙어 있어 이미지 높이에 따라 자리가 움직였다.

```
┌──────────────────────────────────────────────────────────────┐
│ 이미지 텍스트 교체 툴   [파일 선택] 파일명 │ 폰트[▼] │ [PNG 다운로드] │  ← 고정
├────────────────────────────────┬─────────────────────────────┤
│ [체커보드|흰색|검정]  [영역 표시] [원본 보기]│  텍스트 블록 (20)      │
│                                │  [전체][편집 가능][수정됨]   │
│   [ 이미지 미리보기 ]            │  ┌───────────────────────┐  │
│    블록 오버레이                 │  │ ✓ 이벤트 기간 :   [B] │  │
│                                │  │   ~8/31(월)까지   [B] │  │
│                                │  └───────────────────────┘  │
│                                │  선택한 블록                 │
│                                │  [ textarea ]                │
│                                │      [되돌리기] [저장]        │
├────────────────────────────────┴─────────────────────────────┤
│ 형식 PNG·550KB   크기 1140×921   투명 48.8%   반투명 9.7% …    │  ← 고정
└──────────────────────────────────────────────────────────────┘
```

**표시 규칙**

- 블록 배지: `[A]` 투명 배경 · `[B]` 단색 배경 · `[C]` 🔒 편집 불가
- 상태 아이콘: `✎` 저장하지 않은 변경 · `✓` 저장됨 · 없음 = 원본 유지
- C유형 클릭 시: "배경이 복잡해 이번 버전에서는 교체할 수 없습니다" 안내
- 오버레이와 목록은 **양방향**이다. 어느 쪽을 클릭해도 같은 블록이 선택되고,
  오버레이를 클릭하면 목록이 해당 항목으로 스크롤된다

### 6-0. 저장이 곧 결과다 (v1.1 신설)

`[수정 요청]` 버튼을 없앴다. **저장하면 그 자리에서 합성해 미리보기에 반영**한다.

고치고 → 저장하고 → 또 버튼을 눌러야 결과가 보이는 구조에서는, 저장 직후 화면이
그대로여서 "저장이 안 됐나" 싶어진다. 저장과 반영을 한 동작으로 합친다.

- 기본 화면은 **결과**다. 원본은 비교하고 싶을 때만 `[원본 보기]`로 불러온다
- 문구를 다시 고쳐 저장하면 결과가 다시 만들어진다
- 수정된 블록이 하나도 남지 않으면(되돌리기) 결과라는 게 없으므로 원본으로 돌아간다

### 6-7. 비교는 항상 깨끗하게 (v1.2 신설)

- **선택한 블록을 다시 누르면 선택이 풀린다.** 체크를 해제하듯 되돌릴 수단이 있어야 한다
- **`[원본 보기]`/`[결과 보기]`를 누르면 선택이 풀린다.** 전환은 곧 "비교하겠다"는
  뜻이고, 편집 중인 블록의 테두리가 남아 있으면 정작 글자를 못 본다
- 단 **내부 갱신은 선택을 유지한다.** 저장 직후나 되돌리기 후에도 방금 만지던 블록을
  계속 잡고 있어야 이어서 다듬을 수 있다. 선택 해제는 사용자가 버튼을 눌렀을 때만이다

### 6-6. 영역 표시 토글 (v1.1 신설)

원본과 결과를 눈으로 비교하는데 **블록 테두리가 덮여 있으면 정작 글자를 못 본다.**

- `[영역 표시]` **켬**(기본): 편집 가능한 블록을 전부 표시
- `[영역 표시]` **끔**: 선택한 블록 하나만 표시
- 선택까지 풀면(`Esc` 또는 `✕`) **완전히 깨끗한 화면**

### 6-1. 미리보기 배경 토글 (v0.6 신설, M1 구현)

미리보기 배경을 **체커보드 / 흰색 / 검정** 중에서 고를 수 있다.

체커보드 하나로는 부족하다는 것이 M1에서 확인됐다. 샘플 A 1행(흰색 반투명)은
체커보드 위에서도 거의 보이지 않고, **검정 배경으로 바꿔야 비로소 드러난다.**
사용자가 "왜 이 블록이 잡혔지"를 스스로 확인할 수 있어야 하므로 토글이 필요하다.

### 6-5. 합성 조치 안내 (v0.9 신설, M4 구현)

7-8 오버플로 규칙이 발동하면 미리보기 아래에 사유를 칩으로 노출한다
(예: `b1 · 문구가 길어 70%로 축소하고 폭을 넓혔습니다`). 아직 지원하지 않는
유형 B 블록을 수정한 경우에도 건너뛴 사유를 남긴다.

문구를 다시 고치면 **직전 합성 결과는 낡은 것이므로 원본 보기로 되돌린다.**
낡은 결과를 계속 보여주면 사용자가 반영됐다고 착각한다.

### 6-3. OCR 신뢰도 표시 (v0.7 신설, M2 구현)

블록마다 Tesseract가 돌려주는 신뢰도를 함께 보여주고, 70% 미만이면 강조해
"인식 신뢰도가 낮습니다"를 붙인다.

실측에서 **신뢰도가 성공/실패를 꽤 잘 갈랐다.** 정확히 읽은 블록은 86~96%,
실패한 블록은 0~79%였다. 사용자가 어느 블록을 손봐야 하는지 바로 알 수 있다.

### 6-2. 이미지 정보 표시 (v0.6 신설, M1 구현)

업로드 직후 **형식 · 크기 · 투명/반투명/불투명 비율 · 알파 채널 유무**를 보여준다.
투명 비율이 0%이고 알파 채널이 없으면 유형 A 경로를 못 탄다는 뜻이므로, 사용자가
분석을 기다리기 전에 이 이미지가 어느 정도 처리될지 미리 가늠할 수 있다.

## 7. 처리 파이프라인 (핵심 로직)

> v0.3에서 절 번호를 재정렬했다. 배경 유형 판정(7-1)이 이후 모든 단계의 분기
> 기준이라 가장 앞에 둔다.

### 7-1. 배경 유형 판정 (v0.3 신설, 분기의 기준)

블록마다 배경이 어떤 종류인지 먼저 판정한다. 이후 제거 방식(7-5)과 편집 가능
여부가 여기서 갈린다.

> **v0.4에서 알고리즘을 교체했다.** v0.3의 "bbox 바깥 링(ring) 샘플링" 방식은 실제
> 샘플로 검증한 결과 **틀렸다.** 폐기 근거는 아래 "폐기된 링 방식" 참고.

### 판정 절차

**표본은 bbox 바깥이 아니라 bbox 안쪽에서 뽑는다.** 글자 사이의 여백이 곧 그 블록의
진짜 배경이기 때문이다.

**단, 판정용 bbox에는 여유(padding)를 준다** (v0.7 신설). 7-3이 만드는 잉크 bbox는
글자에 딱 붙어 있어 배경 표본이 부족하다. 그대로 판정하면 **단색 배경도 C로 떨어진다** —
검은 원 배지가 실제로 그랬다. 1차는 3px, 2차는 줄 높이의 35% 만큼 넓혀 판정한다.

**1단계 — 투명 배경 판정**

bbox 내부에서 **α ≤ 20 인 픽셀의 비율**을 구한다. 비율 ≥ 15% 이면 **유형 A**.

"배경이 다수인가"가 아니라 **"투명한 부분이 존재하는가"** 를 묻는 것이 핵심이다.
v0.4는 알파 중앙값(사실상 50% 문턱)을 썼는데, 굵은 글자에 타이트한 bbox면 잉크가
면적의 과반을 넘어 무너진다 — M0에서 실제로 샘플 A의 빨간 줄이 이 때문에 오판됐다.
비율 규칙은 문턱이 낮아 그 경우에도 견딘다.

측정된 분리 마진도 넓다. 유형 A 블록은 최소 36.7%, 유형 B 블록은 최대 0.0%였다.

**2단계 — 단색 배경 판정**

1단계를 통과하지 못한 블록에 대해:

1. 알파가 높은(> 200) 픽셀만 모은다
2. 색을 16단위로 양자화해 **최빈색**을 구한다 → 배경 후보색
3. 후보색과의 색거리가 임계값 이내인 픽셀의 비율(= 배경 점유율)을 계산한다
4. 점유율 ≥ 45% **그리고** 그 픽셀들의 RGB 표준편차 < 8 이면 **유형 B**.
   대표색은 그 픽셀들의 **중앙값**
5. 그 외는 **유형 C** — 이번 버전 미지원, 잠금

최빈색을 배경으로 보는 근거는 **블록 안에서 배경이 글자보다 넓다**는 것이다.
검은 원 위 흰 글자든, 연파랑 박스 위 검은 글자든 이 가정은 성립한다.

### 폐기된 링 방식 (v0.3) — 실패 기록

v0.3은 bbox를 3px 바깥으로 확장한 링을 표본으로 삼고, 링의 **평균 알파**와 **RGB
표준편차**로 판정했다. 실제 샘플에 적용하니 다음과 같이 오판했다.

| 블록 | 실제 | 링 방식 판정 | 대표색 |
|------|------|--------------|--------|
| 샘플 A 글자 2행 | A (투명) | **B** ❌ | RGB(226,26,0) = 글자색 |
| 샘플 B 헤드라인 | A (투명) | **B** ❌ | RGB(17,17,17) = 글자색 |

**실패 원리**: bbox가 조금이라도 어긋나면 링이 글자를 문다. 링에 글자가 섞이면
(a) 평균 알파가 올라가 투명 판정을 놓치고, (b) 글자가 단색이면 σ가 0이 나와
**"완벽한 단색 배경"으로 보인다.** 그 결과 대표색이 배경색이 아니라 **글자색**으로
잡히고, 7-5에서 글자 자리를 **글자색으로 칠해버린다** — 즉 화면에 단색 사각형이
남는다. 글자가 단색일수록 더 확실히 틀리는, 최악의 실패 모드였다.

교훈: **배경 표본에 글자가 섞이는 것을 구조적으로 막아야 한다.** bbox 안쪽에서
뽑고 중앙값·최빈값 같은 강건한 통계를 쓰면 글자가 섞여도 배경 쪽으로 수렴한다.

### 기타

- 알파 채널이 아예 없는 이미지(JPG)는 1단계를 건너뛰고 2단계부터 판정한다.
  압축 노이즈 때문에 σ 임계값을 PNG보다 느슨하게 잡는다
- **대표색은 블록마다 따로 구한다.** 샘플 B의 파란 박스는 위치에 따라 RGB가 10 남짓
  변하는 미세 그라데이션이라, 이미지 전역에서 하나의 배경색을 뽑으면 어긋난다
- 판정이 애매하면 **C로 떨어뜨려 잠근다.** 잘못 칠하는 것보다 "이 블록은 안 됩니다"가
  낫다. 실패 방향을 안전한 쪽으로 고정한다
- 판정 결과는 UI에 배지로 그대로 노출한다. **사용자가 되는 것과 안 되는 것을
  미리 아는 것**이 이 단계의 목적이다
- 판정 로직은 `tools/tier-probe.py` 로 재현·검증할 수 있다

### 7-2. OCR 전처리 (필수)

투명 PNG를 캔버스에서 그대로 읽으면 투명 픽셀이 `RGBA(0,0,0,0)`, 즉 **검정**으로
취급된다. 흰색 글자 타이틀이라면 OCR 입장에서 "검정 배경 위 검정 글자"가 되어
인식률이 0에 수렴한다. 배경 유형별로 OCR 입력을 따로 만든다.

**유형 A (투명 배경)** — 알파 채널만으로 흑백 이미지 생성

```
gray = 255 - alpha       // 글자=검정, 배경=흰색
출력 알파는 255로 고정
```

**유형 B (단색 배경)** — 7-1의 대표색과의 색거리로 흑백 이미지 생성

```
d = 대표색과의 RGB 거리
gray = d 가 클수록 검정 (글자), 작을수록 흰색 (배경)
```

이렇게 하면 흰 배경 검은 글자든, 검은 원 위 흰 글자든, 파란 박스 위 검은 글자든
**전부 "흰 바탕에 검은 글자"로 정규화**되어 OCR 조건이 균일해진다.

부수 효과로 그라데이션·장식 색상이 날아가 **12장의 "그라데이션에서 오탈자"
리스크도 함께 줄어든다.

**알파 정규화** (v0.7 신설, 필수). 유형 A에서 `255 - alpha` 를 그대로 쓰면 **반투명
글자가 회색으로 찍혀 대비가 부족**하다. 블록의 대표 불투명도(7-4)로 나눠 정규화한다.

```
v = 255 - min(255, alpha / opacity)
```

샘플 A 1행(α 최대 153)이 이것 하나로 오인식에서 정확 인식으로 바뀌었다.

**확대 배율** (v0.7 실측): 줄 높이가 **64px**가 되도록 확대한다(최대 4배). 실험에서
40px는 굵은 글자를 놓쳤고, 96px는 오히려 나빠졌다. 샘플 B 각주(약 11px)는 확대
없이는 거의 확실히 실패한다.

**페이지 분할 모드는 PSM 6 고정** (v0.7 실측). 한 줄짜리 블록에 PSM 7(단일 행)을
쓰면 더 나빠진다. 여러 줄 블록에 7을 쓰면 아예 무너진다.

인식에 실패하면 원문을 비우고 "인식 실패"로 표시하되, 사용자가 직접 타이핑해
교체하는 것은 허용한다.

### 7-3. 글자 영역 검출

**투영(projection profile)만으로는 부족하다** (v0.7 수정). 샘플 B처럼 좌우 컬럼·배지·
사진이 섞인 레이아웃에서는 가로 투영이 전부 겹쳐 의미를 잃는다. **런렝스 평활(RLSA)
+ 연결요소(connected component)** 로 바꾼다.

검출은 두 갈래로 나뉜다.

**1차 — 알파 마스크 (유형 A)**
- `alpha > 20` 픽셀이 글자
- 가로 26px / 세로 4px RLSA로 글자를 줄 단위로 잇는다
- 연결요소 → 후보 영역 → 실제 잉크로 타이트한 bbox 재계산
- 유형 A로 판정된 것만 채택. 불투명 덩어리는 2차로 넘긴다

**2차 — 단색 패치 안의 글자 (유형 B)**
- 불투명 픽셀의 최빈색들로 **큰 단색 패치**를 먼저 찾는다 (파란 정보 박스, 검은 원 배지)
- 패치 **내부** 픽셀 중 패치색과 충분히 다른 것이 글자
- 같은 RLSA + 연결요소로 줄을 뽑는다

> 잉크를 패치 bbox가 아니라 **패치 마스크 내부**로 제한해야 한다(v0.7). 원형 배지는
> bbox 모서리가 패치 밖이라, 제한하지 않으면 뒤에 깔린 사진이 글자색으로 섞인다.
- 블록 bbox는 **실제 글자 픽셀의 잉크 bbox**로 확정 (OCR bbox는 여유 패딩이 있어
  그대로 쓰면 폰트 크기 역산이 어긋난다)

**한 줄 안의 색 분리** (v1.1 신설). 한 줄에 색이 두 가지 섞여 있으면
(`이벤트 기간 :` 은 파랑, `~8/31(월)까지` 는 검정) 하나로 묶었을 때 지배적인 색으로
통일돼 재현이 틀어진다. 글자 조각을 왼쪽부터 훑으며 색이 바뀌는 지점에서 끊는다.

**색은 '크기'가 아니라 '배경에서 벗어난 방향'으로 비교한다.** 얇은 획은 전부
안티에일리어싱이라 같은 검정이어도 옅게 잡힌다. 절대 색으로 비교하면 굵은 글자와
얇은 글자가 다른 색으로 갈려 **단색 문장이 잘게 찢어진다**. 배경에서 뻗어 나간
방향(단위벡터)은 굵기와 무관하게 같으므로, 진짜로 색이 다를 때만 벌어진다.

```
조각 색의 방향 = 정규화(조각 대표색 − 배경색)
방향 거리가 0.35 를 넘으면 새 구간을 연다
```

보조 규칙:

- **색 기준을 정할 자격은 줄 높이의 45% 이상인 조각에만 준다.** 쉼표·마침표처럼
  키가 작은 조각은 획이 얇아 배경에 물들고, 그 색으로 구간을 열면 문장이 찢어진다
- **자격 없는 조각은 가로로 가장 가까운 구간에 붙인다** (v1.2 수정). 무조건 앞
  구간에 붙이면 `기간 : ~8/31` 의 물결표가 파란 라벨에 딸려 들어가, 값을 고쳐도
  물결표만 원본 자리에 남는다. 실측상 `:`→`~` 간격이 6px, `~`→`8` 이 2px 였다
- 구간의 x 범위가 서로 물리면 중간점에서 자른다. 안 그러면 같은 글자가 두 블록에 들어간다
- **색 추출도 구간의 색 방향에 맞는 픽셀만 쓴다.** 경계에 다른 색이 몇 픽셀만 섞여도
  그쪽이 배경에서 훨씬 멀면 코어 기준을 장악해 정작 그 구간의 색이 통째로 배제된다
  (파란 라벨이 검정으로 잡히던 문제)

**여러 줄 그룹핑** (v0.3 신설): 두 샘플 모두 "2줄 한 덩어리" 구조다. 아래 조건을
모두 만족하는 인접 줄은 하나의 블록으로 묶는다.

- 세로 간격이 줄 높이 이내 — 문단의 줄 간격은 0.86배, 떨어진 라벨끼리는 1.14배로
  측정됐다. 그 사이인 1.0을 경계로 둔다 (v1.1 조정)
- 가로 방향으로 겹치는 구간이 있음
- 추정 글자 크기가 서로 1.3배 이내

묶인 블록의 입력창은 **여러 줄 입력(textarea)** 이며, 줄바꿈이 원본 줄 구성에
그대로 대응한다.

**크기 편차 대응**: 샘플 B는 한 이미지 안에서 글자 크기가 4배까지 차이 난다.
투영 기반 분리의 임계값을 이미지 전역 고정값으로 두면 큰 글자와 작은 글자 중
한쪽이 반드시 깨진다. 임계값은 **각 덩어리의 추정 높이에 비례**해 잡는다.

### 7-4. 색 추출 (그라데이션 대응)

- 각 블록 글자 픽셀 중 상단 25% / 하단 25%의 평균색 → 세로 그라데이션 양 끝 추정
- 단색이면 top≈bottom으로 자연 수렴 (분기 없이 한 경로로 처리)
- 잉크 여부(0/1)가 아니라 **"얼마나 확실한 글자인가"를 가중치**로 쓴다 (v0.7 정리)
  - 유형 A: `alpha / 255`
  - 유형 B: **글자 코어만** — 배경과의 색거리가 그 블록 최대 거리의 65% 이상인
    픽셀만 쓴다 (v0.10 수정)
- 유형 B에서 거리에 *비례한* 가중치를 주면 경계 픽셀이 절반 넘는 무게를 받아
  색이 배경 쪽으로 끌려간다. 검은 원 위 흰 글자가 `rgb(195,194,194)` 회색으로
  잡혔다. 코어만 쓰면 `rgb(249,249,249)` 로 정확해진다

**글자의 불투명도도 함께 추출한다** (v0.5 신설). 글자 자체가 반투명일 수 있다.
잉크 픽셀 알파의 **95번째 백분위**를 대표 불투명도로 삼고, 렌더 시 `globalAlpha`로
적용한다. 최댓값이 아니라 백분위인 것은 외곽 안티에일리어싱 픽셀에 끌리지 않기
위함이다.

> 이 항목은 M0에서 발견됐다. 샘플 A 1행은 α가 최대 153(60%)인 흰색 반투명 글자라,
> 색만 가져가고 불투명도를 빠뜨리면 재렌더 결과가 원본보다 진하게 나온다.

> **알파 가중은 선택이 아니라 필수다** (v0.4 실측). 샘플 B의 각주 블록은 불투명
> 픽셀이 **0%** 이고 글자가 전부 반투명이다. 불투명 픽셀만으로 색을 구하는 구현은
> 이 블록에서 표본이 하나도 없어 그대로 실패한다.

> 실제 샘플 2종은 **모두 단색 글자**였다 — 샘플 A는 RGB(226,26,0) 하나, 샘플 B
> 헤드라인은 RGB(17,17,17) 하나. 그라데이션 대응은 유지하되(비용이 없으므로)
> 검증 우선순위는 낮다.

### 7-5. 글자 제거 (배경 유형별 분기)

**공통 — 제거 마스크 만들기.** bbox 사각형으로 지우지 않는다. 사각형으로 밀면
bbox 안에 겹쳐 있는 장식 요소(밑줄, 도형, 아이콘)까지 함께 사라진다.

1. 블록 내 글자 픽셀 집합을 구한다 (7-3)
2. 이 집합을 2~3px **dilate**(팽창)해 제거 마스크를 만든다
3. 마스크 안의 픽셀만 처리한다

dilate가 필요한 이유는 안티에일리어싱 때문이다. 검출 임계값 이하의 반투명 외곽
픽셀을 남기면 글자 윤곽이 유령처럼 잔상으로 남는다.

**유형 A**: 마스크 안 `alpha = 0`. 배경 복원 불필요.

**유형 B** — 배경을 복원해 덮는다 (v0.10 수정).

> **배경 앵커는 대표색에 가까운 픽셀만 인정한다** (v1.2). "마스크 밖"이라는 조건만
> 쓰면 바로 옆 블록의 글자(파란 라벨의 `:`)를 배경으로 집어 들고, 그 어두운 색에서
> 오른쪽 배경까지 선형 보간해 **번지는 가로줄**을 그린다. 색 분리로 블록이 잘게
> 나뉘면서 이웃 글자가 가까워져 드러난 결함이다.

계획서 v0.3~v0.9는 "대표색 하나로 채우기"였다. 실물에서는 통하지 않는다.
**단색으로 보이는 배경도 위치에 따라 RGB가 10 남짓 변하고**(샘플 B 파란 정보 박스),
한 색으로 칠하면 그 자리가 띠처럼 도드라진다.

대신 **줄 단위로 좌우 배경을 찾아 선형 보간**한다.

```
각 행에서 제거 마스크의 연속 구간을 찾는다
  왼쪽 바깥에서 첫 배경 픽셀 L, 오른쪽 바깥에서 첫 배경 픽셀 R (최대 60px 탐색)
  구간을 L → R 선형 보간으로 채운다 (alpha = 255)
  한쪽을 못 찾으면 반대쪽 색, 둘 다 없으면 7-1의 대표색으로 물러선다
```

배경이 균일하면 결과가 대표색 채우기와 같아지고, 변하면 그 변화를 따라간다.

**페더링은 불필요하다** (v0.10). 제거 마스크가 잉크보다 2px 부풀려져 있어 경계
바깥은 이미 순수 배경이고, 채워 넣는 값도 그 배경에서 보간한 색이라 이음매가
생기지 않는다. 실측으로 확인했다.

> 유형 B는 유형 A 대비 제거 방식만 다르다. 색 추출·피팅·자간·오버플로·렌더는
> 전부 공유한다. 구현 비용은 작지만 커버리지 증가폭은 크다 — 샘플 B가 여기서 열린다.

### 7-6. 새 문구 합성

**폰트 사이즈는 공식으로 역산하지 않고 피팅 루프로 수렴시킨다.** bbox 높이는
실제 글리프의 잉크 높이지만 `font-size`는 em 기준이라 폰트마다 비율이 다르고,
한글은 라틴 대비 글자가 박스를 꽉 채운다. 한 번의 곱셈으로는 안 맞는다.

```
size = lineHeight * 1.2             // 초기 추정
3회 반복:
  렌더 후 measureText()의
  actualBoundingBoxAscent + actualBoundingBoxDescent → 실제 잉크 높이 h'
  size *= lineHeight / h'           // 배율 보정
```

**크기는 블록 안에서 하나로 통일한다** (v0.9 신설). 줄마다 따로 맞추면 **글자 구성에
따라 잉크 높이가 달라져, 원본에서 같은 크기였던 두 줄이 서로 다른 크기로 렌더된다.**
샘플 A 헤드라인이 51.4px / 45.7px 로 갈렸다.

줄별 피팅 결과를 모아 **중앙값 중 큰 쪽**을 블록 크기로 쓴다. 작은 쪽을 고르면 글자가
상자를 못 채워 그 부족분을 자간이 메우게 되고, 그 자간이 새 문구에 그대로 물려가
글자가 벌어져 보인다.

**자간 추정** (v0.3 신설). 한글 타이틀은 자간을 좁힌 경우가 많고, 두 샘플의
헤드라인 모두 그렇다. 자간을 0으로 두고 렌더하면 원본보다 눈에 띄게 넓어진다.

글자 수로 나누지 않는다 (v0.5 수정). 자간이 마지막 글자 뒤에도 붙는지 여부가
구현마다 달라 `글자수 - 1`이 맞지 않는다. **두 점을 측정해 기울기를 구한다.**

```
W   = 원본 줄의 잉크 bbox 폭
W0  = 자간 0으로 렌더했을 때의 폭
W10 = 자간 10px으로 렌더했을 때의 폭
기울기 k = (W10 - W0) / 10        // 자간 1px당 폭 증가량
tracking = (W - W0) / k
```

이렇게 구한 `tracking`을 새 문구에 그대로 적용한다. OCR 원문이 틀리면 이 값도
틀어지므로, 원문이 "인식 실패"인 블록은 자간 0으로 둔다.

**자간에는 상한을 둔다 — 폰트 크기의 ±15%** (v0.9 신설). 판별된 폰트가 원본보다
좁으면 폭을 채우려고 자간이 크게 잡히는데(실측 +15.7%), 그 값을 짧아진 새 문구에
그대로 물려주면 글자가 눈에 띄게 벌어진다. **폰트 불일치가 자간으로 증폭되는 것**을
막는 안전장치다. 폰트가 잘 맞으면 자간은 크기의 ±1% 수준이라 상한에 걸리지 않는다.

이후:
- 선택/판별된 폰트로 오프스크린 Canvas에 렌더
- 세로 그라데이션 색 입힘 (`createLinearGradient`, 잉크 bbox 상단→하단)
- 원위치 합성. **정렬 추정**: 기본은 원본 블록 bbox 중앙 정렬. 단 블록이 여러 개고
  좌측 x좌표가 서로 정렬돼 있으면 좌측정렬로 간주한다

**수정되지 않은 블록은 렌더하지 않는다** (v0.3 신설, 중요). 샘플 B처럼 블록이
20개인 이미지에서 전 블록을 재렌더하면, 사용자가 손대지도 않은 문구까지 폰트
근사 오차로 미세하게 열화된다. **`editedText !== originalText` 인 블록만** 제거·
합성 대상이며, 나머지 영역은 원본 픽셀을 그대로 통과시킨다.

**글리프 부재 감지** (v0.3 신설): 샘플 B 헤드라인의 `♥` 같은 기호는 OCR이 못 읽고,
선택한 웹폰트에 글리프가 없으면 조용히 폴백 폰트로 그려져 모양이 달라진다.
렌더 전에 `measureText`로 해당 문자의 폭이 폴백과 일치하는지 검사해, 의심되면
"이 폰트에 없는 문자가 있습니다" 경고를 띄운다.

### 7-7. 폰트 자동판별 (모드 B)

**판별은 이미지 단위가 아니라 블록 단위로 한다** (v0.5, M0 발견). 한 이미지 안에서도
줄마다 폰트가 다르다. 샘플 A만 해도 1행은 얇은 고딕, 2·3행은 초굵은 디스플레이체다.
이미지 전체에 폰트 하나를 적용하면 얇은 줄이 굵은 폰트로 뭉개지고, 폭을 맞추려
자간이 음수로 밀려 글자가 서로 붙는다. M0 실측으로 재현도가 갈렸다.

| 판별 단위 | 평균 재현도 |
|---|---|
| 이미지 전체 단일 폰트 | 57.2% |
| **블록별 판별** | **73.3%** |

- 후보 웹폰트 각각으로 원문 글자를 렌더 → 이진 실루엣 생성
- 원본 글자 실루엣과 **IoU(겹침 비율)** 비교 → 최고 점수 폰트 선택
- IoU는 **해당 블록 영역으로 한정**해 계산한다. 캔버스 전체로 재면 다른 블록의
  결과가 섞여 판별이 흐려진다

**정규화는 7-6의 피팅이 대신한다** (v0.11 수정). v0.4~v0.10은 "크롭 → 리스케일 →
무게중심 정렬"을 별도 단계로 요구했다. 그 전제는 후보 글자를 **아무 크기·위치로
렌더한 뒤 비교한다**는 것이었다.

지금은 그렇지 않다. 7-6의 크기 피팅과 자간 역산이 후보 글자를 **원본과 같은 상자에
같은 크기로 놓는다.** 즉 렌더 시점에 이미 정규화가 끝나 있고, 여기에 크롭·리스케일을
더하면 오히려 위치 정보를 버리게 된다.

```
후보 폰트로 원문을 7-6 피팅 그대로 렌더 (블록 bbox 크기의 캔버스)
원본 실루엣과 제자리 IoU 계산
```

**판별과 합성이 같은 렌더 경로를 써야 한다**는 점이 중요하다. 다른 방식으로 글자를
놓으면 "판별할 때 닮았던 폰트"가 합성에서 달라진다. `fontmatch.js` 는 `compose.js` 의
피팅·측정 함수를 그대로 가져다 쓴다.

**굵기(weight)까지 후보에 포함** (v0.3 신설). 두 샘플 모두 헤드라인이 매우 굵다
(ExtraBold 계열로 보임). 패밀리만 맞고 굵기가 틀리면 결과가 확연히 달라 보인다.
다만 후보 수가 `폰트 × 굵기`로 늘어나므로 2단계로 좁힌다.

1. **잉크 밀도**(글자 픽셀 수 / 잉크 bbox 면적)로 굵기를 1차 추정 → 상위 2개
   굵기만 남긴다
2. 남은 조합에 대해서만 정규화 IoU 계산

기타:
- 신뢰도 표기는 IoU 절대값 대신 **1위와 2위의 점수 격차**를 함께 본다. 격차가
  4%p 미만이면 "후보 간 차이 작음"을 붙여 낮은 확신을 드러낸다

**IoU 는 굵기 일치를 과대평가한다** (v1.3 발견). 겹친 잉크 '면적'을 재므로 획이
굵은 폰트가 유리하고 글자 '모양' 차이는 덜 반영된다. 샘플 A 헤드라인에서
Black Han Sans(58.6%)가 SUIT 900(47.5%)을 눌렀지만, 눈으로 보면 숫자 `2027` 모양은
SUIT 쪽이 원본에 가깝다. **후보군을 실제 사용 폰트로 한정하는 것**이 이 편향을
피하는 가장 확실한 방법이다(8-1).

**확신이 낮은 블록은 이미지의 지배적 패밀리를 따른다** (v1.3 신설). 짧고 작은
글자는 후보 점수가 고만고만해, 같아 보이는 라벨 셋이 서로 다른 폰트로 갈린다
(실측: `이벤트 기간 :` SUIT 600 / `이벤트 혜택 :` Noto 900 / `이벤트 참여 방법 :`
Pretendard 400). 한 디자인이 여러 패밀리를 섞는 경우는 드무니, 애매할 때는 그
이미지에서 가장 잘 맞은 패밀리를 따르는 편이 덜 튄다.

안전장치 둘:

- **블록이 4개 미만이면 다수결을 적용하지 않는다.** 근거가 얇으면 한 블록이 표를
  독식한다. 샘플 A(2블록)에서 얇은 흰 줄이 헤드라인의 패밀리까지 끌고 갔다
- **점수를 5%p 넘게 깎으면서까지 맞추지 않는다.** 정말 다른 폰트일 수 있다
- 후보군이 좁을수록 정확 (브랜드 폰트 5~6개 등록 권장)
- 이 로직은 **OCR 결과가 정확할 때만 의미가 있다.** 원문이 틀리면 실루엣 자체가
  틀린 글자라 판별이 무너진다 → M5는 M2 품질에 종속됨

### 7-8. 오버플로 규칙

새 문구가 원본보다 길거나 짧은 경우의 처리 규칙. **실사용에서 가장 자주 발생하는
케이스**이므로 명시적으로 정의한다.

문구가 원본 bbox 폭을 넘칠 때, 아래 순서로 적용하고 첫 단계에서 해결되면 멈춘다:

| 순서 | 조치 | 한도 |
|------|------|------|
| 1 | 자간 압축 (7-6에서 추정한 tracking 기준) | 추가 -5% |
| 2 | 폰트 크기 축소 | 원본의 70%까지 |
| 3 | bbox 가로 확장 (정렬 기준점 유지) | 인접 블록/캔버스 경계까지 |
| 4 | 경고 표시 후 그대로 렌더 | — |

- 2·3단계가 적용되면 결과 미리보기에 사유를 노출한다
  (예: "문구가 길어 90%로 축소했습니다")
- **문구가 짧아진 경우 폰트 크기를 키우지 않는다.** 원본 크기를 유지하고 정렬
  기준에 맞춰 재배치만 한다. 크기를 키우면 다른 블록과 시각적 위계가 깨진다
- 3단계에서 **인접 블록 침범 검사**가 필요하다 (v0.3 보강). 샘플 B처럼 블록이
  빽빽한 이미지에서는 캔버스 경계보다 옆 블록에 먼저 부딪힌다
- **줄 수가 바뀌는 경우** (v0.9 신설): 사용자가 줄바꿈을 추가하면 원래 줄 상자보다
  많은 줄이 생긴다. 남는 줄은 마지막 상자 아래로 **원래 줄 간격만큼** 이어 붙인다.
  줄이 줄면 남은 상자는 지워진 채로 둔다

### 7-9. 타이포 계측 (v1.5 신설)

"유사도 67%" 하나로는 담당자가 판단할 수 없다. **모양이 안 닮은 것**과 **글자가
옆으로 밀리는 것**은 전혀 다른 문제인데 한 숫자로 뭉치면 구분이 안 된다.
디자인팀이 자간·행간을 손으로 만진다고 한 이상 그 값 자체도 보여야 한다.

**원문을 다시 그려 원본과 맞춰 본다.** 편집 결과가 아니라 원문이다. 원문조차
제자리에 못 놓으면 편집 결과는 당연히 못 놓는다.

| 항목 | 재는 방법 | 실측 분포 (샘플 B 20블록) |
|------|-----------|---------------------------|
| 폰트 | 실루엣 IoU | 12~87%, 중앙값 47% |
| 크기 | 렌더 잉크 높이 vs 원본 줄 높이 | 94~100% |
| 자간 | 렌더 잉크 폭 vs 원본 줄 폭 | 47~100% |
| 정렬 | 렌더 잉크 왼쪽 x vs 원본 줄 왼쪽 x | 100% |
| 행간 | 원본 줄 간격을 그대로 재사용 — **점수를 매기지 않는다** | 1.35~1.90배 |

총합 = 폰트 .40 + 크기 .20 + 자간 .25 + 정렬 .15. **행간은 총합에서 뺀다.**
줄 간격은 원본 값을 그대로 쓰므로 재현 오차가 없다. 100%라고 적으면 계산에
기여하지 않으면서 총합만 올리는 가짜 만점이 된다.

색 기준을 항목마다 다르게 둔다. IoU 는 정답 폰트를 써도 80%를 넘기 어렵고
(중앙값 47%), 크기·자간·정렬은 맞으면 100%가 나온다. 같은 잣대를 대면 폰트는 늘
빨갛고 나머지는 늘 초록이라 색이 아무 정보도 주지 않는다.

`compose.js` 의 `layout()`·`inkX()` 를 그대로 쓴다. 계측이 따로 계산하면
"판별 화면에서 본 수치"와 "실제로 찍히는 글자"가 갈린다.

### 7-10. 정렬 추정 재작성 (v1.5)

**예전에는 한 줄짜리 블록을 무조건 `center` 로 봤다.** 그게 편집 후 글자가 옆으로
밀리는 원인이었다. 가운데 정렬은 문구 길이가 바뀌면 **차이의 절반만큼 양옆으로
흔든다.** 실측으로 확인했다:

| 블록 | 원문 | 한 글자 지웠을 때 왼쪽 끝 이동 |
|------|------|-------------------------------|
| b14 | `~8/31(월)까지` | 가운데 5.6px → **왼쪽 0.0px** |
| b13 | `이벤트 기간 :` | 가운데 3.9px → **왼쪽 0.0px** |
| b11 | (인식 실패) | 가운데 18.0px → **왼쪽 0.0px** |

한 줄에는 정렬을 알아낼 근거가 없다. **근거가 없을 때 흔들리지 않는 쪽을 고르는
것이 맞다** — 왼쪽이다. `center` 는 증거가 있을 때만 준다:

1. 두 줄 이상 — 줄들의 왼쪽 끝 / 오른쪽 끝 / 중심 중 가장 안 흩어진 기준을 따른다
2. 한 줄 — 왼쪽에 같은 행의 다른 블록이 60px 안에 붙어 있으면 `left`
   (`이벤트 기간 :` 다음의 값)
3. 한 줄 — 이미지 한가운데(±2%)에 놓였으면 `center`
4. 그 외 `left`

가운데 정렬이 남는 블록(샘플 B 에서 2개)은 **밀리는 게 맞다.** 원본이 실제로
가운데 정렬이면 길어진 문구는 양쪽으로 퍼져야 한다. 숨길 게 아니라 알려 줄 일이라
계측이 이동량(px)을 재서 패널에 적는다.

정렬은 이미지 전체를 봐야 정해지므로(왼쪽 이웃·이미지 중심) 판별이 끝난 뒤 한 번
계산해 `block.align` 에 박아 둔다. 합성·판별·계측이 모두 그 값을 쓴다.

## 8. 폰트 처리 — 사용자 선택 UX

라디오로 두 모드 제공:

- **직접 선택**: 등록된 웹폰트 후보를 드롭다운으로 노출. 미리보기에 즉시 반영.
  아는 사용자가 정확히 지정.
- **자동판별에 맡기기(기본값)**: 7-7 로직으로 가장 닮은 폰트를 자동 선택.
  결과 옆에 "판별: OO (유사도 82%)" 를 표기해 신뢰도를 보여줌.

> 폰트가 정확히 안 맞아도 **교체 자체는 항상 성공**한다(M0로 확인). 자동판별은
> "더 자연스럽게" 만드는 보조 장치이지 성패를 가르는 요소가 아님.

### 8-1. 등록 폰트 후보군 (v1.3 재구성)

**전부 SIL Open Font License 1.1.** OFL은 임베드·재배포·상업적 이용을 모두 허용하므로
**외부 공개 배포에 제약이 없다.** 13장의 폰트 라이선스 리스크는 이 선택으로 닫힌다.

#### 자동판별 후보 — 디자인팀이 실제로 쓰는 세 패밀리

| 패밀리 | 굵기 | 출처 |
|--------|------|------|
| **SUIT** | 400·500·600·700·800·900 | github.com/sun-typeface/SUIT |
| **Pretendard** | 400·500·600·700·800·900 | github.com/orioncactus/pretendard (subset) |
| Noto Sans KR | 400·700·900 | Google Fonts (korean 서브셋) |

**후보를 좁힌 것이 핵심이다.** 넓히면 정답이 아닌 폰트가 IoU 만으로 이긴다 —
7-7의 편향 항목 참고. 실제로 쓰는 폰트만 후보에 두는 것이 그 편향을 피하는 가장
확실한 방법이다.

한글 커버리지는 SUIT 2,668자 · Pretendard 2,780자로 현대 한국어 텍스트를 사실상
전부 담는다. 파일은 패밀리당 160~270KB다.

#### 직접 선택 전용 — 자동판별에서는 제외

Gothic A1(400·700·900) · IBM Plex Sans KR(400·700) · Nanum Gothic(400·700·800) ·
Black Han Sans · Jua.

드롭다운에는 `(판별 제외)` 표시와 함께 남겨 둔다. **자동판별을 좁히는 것과
사용자가 고를 수 있는 폭을 좁히는 것은 다른 문제다.** 미리 받지 않고 고를 때
로드하므로 최초 전송량에 들어가지 않는다.

브랜드 전용 폰트를 추가할 때는 그 폰트의 라이선스를 별도로 확인해야 한다.

### 8-2. 유료 폰트 — 내 PC 폰트 (v1.4 신설)

샘플 A 헤드라인은 **산돌에서 구매한 유료 폰트**다. 세 패밀리 중 어느 것도 아니고,
번들할 수도 없다.

#### 기각한 두 가지

| 방안 | 기각 사유 |
|------|-----------|
| 폰트 파일을 저장소에 넣는다 | 공개 배포되는 앱이다. 저장소에 넣는 순간 재배포가 된다 |
| 글자 모양을 보고 다시 그린다 | 그건 새 폰트가 아니라 복제본이다. 라이선스 우회 목적이면 파생물이고, 한글 2,700자를 눈대중으로 맞출 수도 없다 |

#### 채택 — `local()` 로 설치된 폰트를 불러 쓴다

폰트를 산 사람의 PC에는 그 폰트가 **이미 설치돼 있다.** 브라우저에게 그 자리에서
불러 캔버스에 그리게 한다. 서버도 저장소도 폰트 파일을 갖지 않는다. 재배포가
아니라 **각자 산 폰트를 각자 쓰는 것**이다.

```js
const face = new FontFace('LF ' + ps, `local("${ps}")`);
await face.load();          // 설치돼 있지 않으면 NetworkError 로 거부된다
document.fonts.add(face);
```

실측으로 확인한 성질 두 가지:

- **`load()` 가 설치 여부 검사를 겸한다.** 없는 이름은 `NetworkError` 로 거부된다.
  따로 확인할 필요가 없다.
- **네트워크 요청이 0건이다.** 등록 전후 요청을 세어 확인했다(부록 I). 폰트 파일이
  오가지 않는다는 것이 수치로 남는다.

등록한 폰트는 **자동판별 후보에 들어간다.** 직접 선택으로만 쓰게 하면 반쪽이다 —
직접 선택은 전 블록에 같은 폰트를 씌우는데, 헤드라인만 유료 폰트고 본문은 SUIT 인
이미지가 흔하다. 후보에 있어야 블록마다 갈린다.

가족 이름은 `LF <PostScript 이름>` 별칭을 쓴다. 번들 폰트와 겹치지 않게 하려는 것이고,
표시는 `<이름> · 내 PC` 로 따로 한다. 다수결(7-7)이 묶는 단위는 `group` 필드로 따로
두었다 — 같은 폰트의 Regular/Bold 는 별칭이 달라도 한 묶음이어야 한다.

#### 이름을 고르는 두 경로

1. **목록에서 고르기** — `queryLocalFonts()`. Chromium 계열 + 보안 컨텍스트에서만
   되고 권한 승인도 필요하다.
2. **이름 직접 입력** — 항상 열려 있다. 등록 자체는 열거 API 와 무관하다.

**2를 부차적 수단으로 두지 않았다.** Firefox·Safari 에는 1이 아예 없고, 헤드리스
크로미움은 API 는 있는데 목록을 하나도 안 돌려준다(실측). 열거가 막히는 경우가
예외가 아니라 흔한 쪽이라, 열거 실패는 오류가 아니라 2로 안내한다.

선택은 `localStorage` 에 이름만 남긴다. 다시 등록하는 데는 권한이 필요 없으므로
새로고침하면 조용히 복구된다. 폰트를 지웠거나 다른 PC 면 그냥 빠진다.

## 9. 기술 스택

| 역할 | 선택 | 비고 |
|------|------|------|
| 호스팅 | GitHub Pages | 정적, 무료 |
| 마크업/로직 | Vanilla HTML/CSS/JS | 프레임워크 불필요, 의존성 최소 |
| OCR | **Tesseract.js** | 한국어 `kor` + `eng` traineddata, word/line bbox 제공 |
| 이미지 처리 | **Canvas 2D API** | 알파 조작·색 추출·글자 렌더·합성 전부 담당 |
| 폰트 | 웹폰트(woff2) 임베드 | 브랜드 폰트/공개 웹폰트 후보군 등록 |
| 다운로드 | `canvas.toBlob()` → `a[download]` | PNG 저장 (투명도 보존) |

> 앞서 파이썬 Pillow로 검증한 파이프라인(검출→색추출→제거→합성→폰트판별)을
> 브라우저 Canvas로 1:1 이식하는 구조. 알고리즘은 이미 PoC로 입증됨.

### 9-1. Tesseract.js 자산 배치

Tesseract.js는 CDN 스크립트 외에 **worker 스크립트·wasm 코어·traineddata** 세
가지를 별도로 받는다. GitHub Pages 환경에서 경로/CORS 문제가 생기기 쉬우므로
초기화 시 세 경로를 명시적으로 지정한다.

**자체 호스팅으로 확정했다** (v0.7). CDN 폴백이 아니라 이쪽이 기본이다. 실제 개발
환경에서 jsDelivr·unpkg·tessdata.projectnaptha 가 **전부 차단**돼 있었다. CDN 가용성을
전제하면 그런 환경에서 앱이 통째로 죽는다.

`vendor/tesseract/` 구성 (합계 약 17MB):

| 파일 | 용도 |
|------|------|
| `tesseract.min.js` · `worker.min.js` | 라이브러리 · 워커 |
| `tesseract-core-lstm.wasm.js` | 코어 (기본) |
| `tesseract-core-simd-lstm.wasm.js` | 코어 (SIMD) |
| `tesseract-core-relaxedsimd-lstm.wasm.js` | 코어 (relaxed SIMD) |
| `lang/kor.traineddata` · `lang/eng.traineddata` | 언어 데이터 (tessdata_fast) |

- **코어는 세 변종을 모두 넣어야 한다.** 브라우저가 SIMD 지원 수준에 따라 골라
  가져가며, 없는 변종을 요청하면 그대로 실패한다
- 언어 데이터는 `tessdata_fast` 를 쓴다. `kor` 이 1.7MB로 작고 실측 정확도도 충분했다
- 압축본이 아니므로 `gzip: false` 로 초기화한다
- **최초 1회 로딩 진행률 UI는 그대로 필요**하다. 한 번 받은 뒤에는 브라우저 캐시가 처리한다

## 9-2. 배포 (v1.0 신설, M6 구현)

빌드 과정이 없다. 정적 파일을 그대로 올린다.

**배포물** — `index.html` · `.nojekyll` · `css/` · `js/` · `fonts/` · `vendor/` (21.7MB).
검증용 디렉터리(`tools/` · `spike/` · `assets/`)는 뺀다. 앱 동작에 필요 없고 3.7MB를
차지한다. `.github/workflows/pages.yml` 이 이 추리기를 자동으로 한다.

**경로는 전부 상대경로여야 한다.** Pages 는 저장소 이름 하위 경로
(`/image-text-swap/`)로 서빙되므로 절대경로가 하나라도 섞이면 배포에서만 깨진다.
`tools/deploy-probe.js` 가 그 하위 경로를 흉내 내 전 과정을 돌려 확인한다.

**최초 1회 전송량 13.5MB** (무압축 로컬 측정 기준). 판별 제외 폰트는 미리 받지 않는다.

| 항목 | 크기 |
|------|------|
| `eng.traineddata` | 3.92MB |
| Tesseract 코어 (relaxedsimd) | 3.72MB |
| `kor.traineddata` | 1.60MB |
| 웹폰트 (자동판별 후보 15종) | 4.1MB |

- **코어는 세 변종 중 하나만 전송된다.** 브라우저가 SIMD 지원 수준에 따라 골라가므로
  저장소에는 셋 다 있어야 하지만 사용자가 받는 것은 하나뿐이다
- 실제 Pages 는 gzip 을 적용하므로 전송량은 이보다 줄어든다
- 한 번 받으면 브라우저 캐시가 처리한다

## 10. 데이터 모델

`app.js`가 들고 있을 상태의 형태. 모듈 간 계약을 먼저 고정해 두면 M2~M5를
독립적으로 붙일 수 있다.

```js
state = {
  sourceImage,          // ImageBitmap — 원본
  hasAlpha,             // 알파 채널 존재 여부 (JPG면 false)
  blocks: [
    {
      id,
      bbox: {x, y, w, h},     // 잉크 bbox (7-3)
      lines: [{bbox, text}],  // 묶인 줄 단위 (7-3 그룹핑)
      bgType: 'A',            // 'A' 투명 | 'B' 단색 | 'C' 복잡 (7-1)
      bgColor,                // 유형 B의 대표색
      locked: false,          // 유형 C면 true
      originalText,           // OCR 원문 ('' 이면 인식 실패)
      editedText,             // 확정된 문구. 합성(M4)이 쓰는 값
      draft,                  // 편집 중인 값. 저장 전에는 editedText 와 다르다
      dirty: false,           // editedText !== originalText → 합성 대상
      confidence,             // OCR 신뢰도 (6-3)
      colorTop, colorBottom,  // 그라데이션 양 끝 (7-4)
      silhouette,             // 정규화된 이진 실루엣 (7-7용)
      align: 'center',        // 'center' | 'left'
      fittedSize,             // 피팅 루프 결과 (7-6)
      tracking,               // 추정 자간 (7-6)
      overflowNote: null      // 오버플로 조치 사유 (7-8)
    }
  ],
  fontMode: 'auto',     // 'auto' | 'manual'
  selectedFont,         // manual 모드일 때
  detectedFont: {name, weight, score, runnerUpGap}   // auto 모드 결과
}
```

## 11. 프로젝트 구조

```
image-text-swap/
├── index.html              # 단일 페이지 UI
├── PLAN.md                 # 이 문서
├── css/
│   └── style.css
├── js/
│   ├── app.js              # 플로우 오케스트레이션·상태관리
│   ├── ocr.js              # Tesseract.js 래퍼 (전처리·추출·bbox)
│   ├── detect.js           # 배경유형 판정·알파/색거리 검출·블록 분리·색 추출
│   ├── compose.js          # 제거(A/B)·렌더·자간·합성·오버플로·다운로드
│   └── fontmatch.js        # 폰트/굵기 후보 렌더·정규화·IoU 판별
├── fonts/                  # 웹폰트 후보 woff2 13종 + fonts.css (8-1)
├── vendor/                 # (폴백 시) tesseract worker·wasm·traineddata
├── tools/                  # 검증 도구. 앱 코드·배포물 아님
│   ├── tier-probe.py       #   배경 유형 판정(7-1) 검증
│   ├── screenshot.js       #   샘플 업로드 후 화면 캡처 (마일스톤 판정용)
│   └── shots/              #   캡처 결과
├── spike/                  # M0 스파이크. M1에서 js/ 로 정리하며 폐기
│   ├── m0.html             #   제거→재합성 파이프라인 (Canvas)
│   ├── run.js              #   헤드리스 실행기
│   └── out/                #   결과 이미지·metrics.json
├── assets/
│   ├── sample-simple.png   # 기준 샘플 A (4장)
│   └── sample-complex.png  # 기준 샘플 B (4장)
└── README.md
```

> `tools/` 와 `spike/` 는 배포물이 아니라 **기획 검증용**이다. GitHub Pages에 올라가는
> 것은 `index.html` · `css/` · `js/` · `fonts/` · `vendor/` 뿐이다.

## 12. 구현 마일스톤

각 마일스톤은 **완료 판정 기준**을 함께 둔다. 판정은 4장의 기준 샘플로 한다.

| # | 이름 | 내용 | 완료 판정 |
|---|------|------|-----------|
| ~~M0~~ | ~~스파이크~~ | 샘플 A에 대해 문구 하드코딩으로 "제거 → 재합성"만 수동 실행 | ✅ **통과** — 부록 B |
| ~~M1~~ | ~~뼈대~~ | 업로드 + 원본 미리보기(체커보드) + Canvas 로드 | ✅ **완료** — 두 샘플 모두 표시, 콘솔 오류 없음 |
| ~~M2~~ | ~~추출~~ | 배경유형 판정 + 전처리 + 블록 분리·그룹핑 + OCR → 블록 목록 | ✅ **완료** — 부록 C |
| ~~M3~~ | ~~편집/저장~~ | 블록 오버레이 선택 UI + textarea 수정·저장 상태관리 | ✅ **완료** — 부록 D |
| ~~M4~~ | ~~합성 (유형 A)~~ | 색 추출 → 알파 제거 → 피팅·자간 → 합성 + 오버플로 | ✅ **완료** — 부록 E |
| ~~M4.5~~ | ~~합성 (유형 B)~~ | 배경색 샘플링 → 채우기 → 페더링 | ✅ **완료** — 부록 F |
| ~~M5~~ | ~~폰트~~ | 직접 선택 드롭다운 + 블록별 자동판별(IoU) + 신뢰도 표기 | ✅ **완료** — 부록 G |
| ~~M6~~ | ~~마감~~ | 원본/결과 토글, PNG 다운로드, GitHub Pages 배포 | ✅ **완료** — 부록 H |

### M0을 최우선에 두는 이유

**이 프로젝트의 품질 상한선을 결정하는 것은 OCR이 아니라 합성 결과다.**

- OCR이 틀리면 → 인풋박스 수동 수정으로 덮인다 (설계된 안전망)
- 합성이 어색하면 → **덮을 방법이 없다**

따라서 문구를 하드코딩해서라도 "지우고 다시 그린 결과가 원본처럼 보이는가"를 반나절
안에 먼저 확인한다. 여기서 결과가 납득되지 않으면 M2 이후를 만들 이유가 없다.
대상은 가장 단순한 **샘플 A**로 한다.

### M4를 A/B로 쪼갠 이유

유형 A(투명)와 유형 B(단색)는 제거 로직만 다르고 나머지(색 추출·피팅·자간·합성)를
전부 공유한다. A를 먼저 완성해 공유 경로를 검증한 뒤 B는 제거 분기만 추가하면
되므로, 한 번에 만들다 두 경로가 얽히는 것보다 안전하다.

## 13. 한계 · 리스크

- **블록 bbox 정확도가 판정 품질을 좌우한다** (v0.4). 7-1은 bbox 안쪽에서 표본을
  뽑으므로, bbox가 실제 글자보다 크게 잡히면 옆 요소가 섞여 C로 떨어진다. 샘플 B의
  `추첨 10명` 배지가 그 예다. 7-3의 잉크 bbox 정확도가 곧 7-1의 정확도다
- **유형 B 채우기가 티 날 수 있다** (v0.4 실측). 샘플 B 파란 박스는 완전 단색이
  아니라 위치에 따라 RGB가 10 남짓 변한다. 블록 높이가 작으면 단색 채우기로 충분하지만,
  큰 블록에서는 경계가 보일 수 있다. M4.5에서 실물로 확인할 항목
- **샘플 B의 최종 커버리지는 M4.5에서 확정된다.** 4장 유형 표는 대표 블록만 측정한
  값이고, 전체 블록은 7-3 검출을 붙인 뒤에야 나온다. **되는 블록/안 되는 블록을
  정확히 구분해 알려주는 것**까지가 이번 버전의 약속이다
- **JPG 입력**: 알파 채널이 없어 유형 A 경로를 못 탄다. 전부 B/C로 판정되며,
  JPG 압축 노이즈 때문에 단색 판정의 σ 임계값을 PNG보다 느슨하게 잡아야 한다.
  (기준 샘플 2종은 모두 RGBA PNG라 이 경로는 미검증)
- **브라우저 한글 OCR 정확도**: Tesseract.js `kor`는 장식 폰트에서 오탈자 가능 →
  **인풋박스 수동수정이 안전망**. 7-2 전처리·업스케일로 완화되지만 완전 해결은 아님.
  샘플 B의 각주 크기(~11px)는 실패를 기본 가정으로 둔다
- **traineddata 용량**: 로딩 시간 → 최초 1회 진행률 UX 필요. CDN 차단 대비 자체
  호스팅 폴백(9-1)
- ~~폰트 라이선스~~ → **해소됨** (v0.5). 후보군을 전부 OFL로 구성했다(8-1). 브랜드
  전용 폰트를 추가할 때만 그 폰트의 라이선스를 별도 확인하면 된다
- ~~한 줄에 색이 두 가지면 하나로 통일된다~~ → **해소됨** (v1.1). 7-3의 색 분리로
  라벨과 값이 각각 별도 블록이 된다. 대신 **편집 단위도 함께 쪼개진다** —
  `이벤트 기간 : ~8/31(월)까지` 를 통째로 바꾸려면 두 블록을 각각 고쳐야 한다.
  색 재현의 정확도와 맞바꾼 것이다
- **회전된 글자는 검출·인식 모두 실패한다** (v0.7, M2 관찰). 샘플 B의 배달의민족
  상품권 텍스트가 기울어져 있어 신뢰도 0~79%로 무너졌다. 축 정렬을 전제한 구조라
  이번 버전에서 해결하지 않는다. 낮은 신뢰도로 표시되므로 사용자가 알 수는 있다
- **사진 위에 걸친 단색 도형은 놓친다** (v0.7, M2 관찰). `추첨 10명`·`추첨 3명`
  배지는 원이 사진과 겹쳐 단색 패치로 잡히지 않았다. 같은 모양인 `추첨 5명`은
  잡혔다. 검출 여부가 배경에 따라 갈리는 셈이라 사용자에게는 일관돼 보이지 않는다
- ~~샘플 A 헤드라인의 폰트는 후보에 없다~~ → **해소됨** (v1.4). 산돌에서 산 유료
  폰트여서 번들할 수 없었다. 8-2의 '내 PC 폰트'로 푼다 — 폰트를 산 사람의 PC에
  설치된 것을 `local()` 로 불러 자동판별 후보에 넣는다. **다만 그 PC에서만 맞는다.**
  폰트가 없는 사람에게는 후보에 안 뜨고 근사치로 떨어진다. 결과 이미지는 그림이라
  누구에게나 같게 보이므로, 편집하는 사람만 폰트를 갖고 있으면 된다
- **후보군에 정답 폰트가 없으면 블록마다 다른 근사치가 뽑힌다** (v0.5, M0 관찰).
  샘플 A의 1·2행은 육안으로 같은 폰트인데 판별은 `Gothic A1 900` / `Black Han Sans`로
  갈렸다. 결과가 원본보다 덜 일관돼 보일 수 있다. 후보군을 실제 사용 폰트에 가깝게
  등록하는 것이 근본 해결이며, 그 전까지는 '직접 선택' 모드가 우회로다
- **보이지 않는 블록**: 샘플 A 1행처럼 흰 배경에서 안 보이는 반투명 글자가 있다.
  툴은 정상 검출하지만 사용자는 "왜 이게 잡혔지"가 된다. 체커보드 미리보기와
  블록 오버레이 강조로 위치를 알려주는 것이 필수다
- **효과 있는 글자**(외곽선·그림자): 스코프 밖. 외곽선이 있으면 7-4 색 추출이
  외곽선 색에 오염된다
- **블록 분리 오검출**: 이번 버전에서는 문구 수정으로만 대응, 수동 병합/분리는 다음 단계
- **폰트 로딩 타이밍**: 웹폰트 로드 전에 캔버스에 렌더하면 폴백 폰트로 그려진다.
  합성·판별 전 `document.fonts.ready` 대기 필수 (조용히 틀리는 종류의 버그)
- **저장 형식**: 원본이 JPG여도 결과는 PNG로 내보낸다. 재압축 열화를 피하기 위함이며,
  파일 크기가 커지는 것은 감수한다
- **최초 로딩이 무겁다** (v1.0). 14.3MB를 받아야 첫 분석이 시작된다. 사내 사용에는
  문제없지만 외부 공개 시에는 체감될 수 있다. 줄이려면 `eng.traineddata` 를 빼는 것이
  가장 크지만, 실측 결과 영문이 전부 깨져(`EVENT`→`ㄷ66411`) 채택할 수 없었다

## 14. 착수 전 확정 필요 사항 — 전부 해소됨 (v0.5)

1. ~~테스트용 샘플 이미지~~ → **완료.** `assets/`에 원본 2종 반영, 4장에 실측값 기재
2. ~~브랜드 웹폰트 후보 목록~~ → **완료.** OFL 6패밀리 13종 확정 (8-1)
3. ~~외부 공개 여부~~ → **외부 공개 허용으로 결정.** 후보군을 전부 OFL로 구성해
   라이선스 제약을 원천 제거했다. 향후 브랜드 전용 폰트를 추가할 때만 재검토한다

착수를 막는 항목은 남아 있지 않다.

## 15. 향후 확장
- **사진·그라데이션 배경 대응** (유형 C) — 인페인팅 단계 추가
- 외곽선/그림자 레이어 분리·재현
- 블록 수동 병합/분리 보정 UI
- 브랜드 폰트 사전 자동 등록·관리 화면
- 배치 처리(여러 이미지 일괄 교체)
- 회사 디자인 원본(피그마 API) 연동 A안과 하이브리드

## 16. Claude Code 착수 프롬프트 (예시)

```
타이틀/이벤트 이미지의 글자를 교체하는 정적 웹 앱을 만들어줘.
- 서버 없이 GitHub Pages에 배포되는 순수 HTML/CSS/JS (프레임워크 X)
- 이 기획서(PLAN.md)의 섹션 5 플로우와 섹션 11 구조를 그대로 따를 것
- OCR은 Tesseract.js(kor+eng), 이미지 처리는 Canvas 2D
- 배경 유형 판정(7-1)이 모든 분기의 기준이다. 유형 A/B만 처리하고 C는 잠근다
- OCR 전에 섹션 7-2의 전처리를 반드시 넣을 것
- 수정하지 않은 블록은 원본 픽셀을 그대로 보존할 것 (7-6)
- 먼저 M0(샘플 A에 대해 문구 하드코딩 상태로 제거→재합성)만 만들고 멈춰서
  결과를 보여줘. 합성 품질이 납득되면 그때 M1~M2로 넘어간다.
```

---

## 부록 A. 변경 이력

### v1.7 → v1.8 (OCR 인식률)

포크·수저 아이콘을 글자로 읽고, `핵집` 을 `HS` 로 읽는다는 제보. 정답을 손으로
적어 두고 글자 단위 정확도로 재며 조건을 하나씩 시험했다.

| # | 항목 | 변경 내용 | 근거 |
|---|------|-----------|------|
| 1 | 7-2 전처리 | **바꾸지 않음** | 확대(96·128px)·이진화 전부 더 나빴다. 현재 97.3% 가 최적 |
| 2 | **7-2 한글↔라틴 교정** | 한글 블록의 저신뢰 라틴 낱말을 kor 단독 결과로 교체 | `핵집→“HS`, `핵심집약→BYU`. 평균 97.3% → **98.3%** |
| 3 | 7-2 | 교체 조건에 "완성형 한글이 나올 때만" 을 넣음 | 신뢰도만으로는 정답 `UP!`(16)과 오답 `BYU`(23)를 못 가른다 |
| 4 | 7-2 | 낱말을 다시 잇지 않고 원문에서 그 낱말만 치환 | 이으면 `핵 1 권 만` 처럼 띄어쓰기가 깨진다 |
| 5 | **11장 `글자 아님`** | 그림으로 보이는 블록을 표시하고 '편집 가능'에서 뺀다 | 아이콘 획 비 1.7·1.8 vs 글자 2.3~4.3 (부록 P) |
| 6 | **11장 미리보기 맞춤** | 캔버스를 가로·세로 양쪽에 맞춘다 (상자에 원본 비율) | `max-width` 만 걸려 세로가 긴 이미지가 잘렸다. 실측 스테이지 819px 에 캔버스 921px |
| 7 | 11장 | 좁은 화면(≤900px)에서 미리보기 패널 높이 확보 | `.pane { min-height: 0 }` 이 뒤에 나와 미디어 쿼리를 이겼다 |

### v1.6 → v1.7 (넘칠 때의 처리 재작성)

`합격`→`불합격`, `50`→`60` 을 넣었더니 낱말 사이가 사라지고 획이 얇아졌다.
7-8 의 순서 자체가 틀렸다.

| # | 항목 | 변경 내용 | 근거 |
|---|------|-----------|------|
| 1 | **7-8 순서** | 자간 압축 → 폰트 축소 → 폭 확장 **→** 폭 확장 → 자간 압축 → 폰트 축소 | 블록 상자는 원문 잉크의 크기일 뿐 레이아웃 상자가 아니다. 옆이 1221px 비었는데도 압축했다 |
| 2 | **7-8 압축량** | 고정 5% → **필요한 만큼만** (기울기 역산) | 2px(0.1%) 넘쳤는데 줄 전체를 83px 좁혔다 |
| 3 | 7-8 `usableWidth` 신설 | 정렬별로 쓸 수 있는 폭을 다르게 잰다 | 가운데 정렬은 좁은 쪽이 한계다. 안 나누면 한쪽 이웃을 밟는다 |
| 4 | 7-8 | 원래 영역보다 넓어지면 알린다 (2% 초과 시) | 조용히 넓어지면 사용자가 모른다 |
| 5 | **7-8 `bgExtent` 신설** | 유형 B 는 바탕이 이어지는 데까지만 넓힌다 | `roomFor` 는 글자 블록만 본다. 원 배지 옆을 948px 비었다고 봐 원 밖으로 나갈 수 있었다 (부록 O) |

### v1.5 → v1.6 (고해상도 검출 · 말 고치기)

2280px 헤드라인을 시험하다 검출이 무너졌다. 상수가 픽셀 고정이었던 것이
드러났고, 같이 UI 용어도 손봤다 — 담당자는 디자이너가 아니다.

| # | 항목 | 변경 내용 | 근거 |
|---|------|-----------|------|
| 1 | **7-3 `rlsaAuto`** | 가로 평활 거리를 글자 높이에 비례시킨다(0.5배, 늘리기만) | `RLSA_X=26` 고정이라 80px 글자에서 낱말이 안 이어져 한 줄이 3조각으로 갈렸다 |
| 2 | **7-3 `LINE_RATIO`** | 한 블록으로 묶을 줄 높이 비 1.3 → **1.15** | 진짜 여러 줄 블록은 실측 최대 1.071. 크기 다른 헤드라인 두 줄(1.22)이 묶여 둘 다 망가졌다 |
| 3 | 11장 | 유형 문자 `A`/`B`/`C` 를 화면에서 뺐다 | 내부 용어다. 사용자에게 아무 뜻이 없다 |
| 4 | **11장 안전도** | 블록마다 `안전`/`글꼴 다름`/`문구 확인`/`위험`/`편집 불가` | 담당자가 목록을 훑을 때 필요한 건 백분율이 아니라 판정이다 |
| 5 | 11장 | 판정 문장을 편집창 맨 위 색 박스로 | 제일 중요한 한 줄이 제일 작고 흐렸다 |
| 6 | 11장 | 수치 패널을 입력창 **아래**로 옮기고 접었다 | 위에 두면 큰 이미지에서 입력창이 화면 밖으로 밀린다 |
| 7 | 7-9 | 용어를 바꿨다 — 폰트→글꼴, 자간→글자 사이(자간), 행간→줄 사이(행간), 정렬→가로 위치 | "타이포 계측 · 원문 재현 기준"은 담당자가 못 알아듣는다 |
| 8 | 7-2 | PSM 7 재검토 → **기각 유지** | 한 줄 블록 18개 중 17개가 PSM 6 과 동일, 나머지 1개는 더 나빴다 |

### v1.4 → v1.5 (타이포 계측 · 정렬 · 조작 정리)

편집 후 글자가 옆으로 밀린다는 제보에서 시작했다. 원인은 한 줄 블록을 무조건
가운데 정렬로 본 것이었다. 고치면서 "왜 다른지"를 항목별로 보여 주는 계측을 붙였다.

| # | 항목 | 변경 내용 | 근거 |
|---|------|-----------|------|
| 1 | **7-10 정렬 재작성** | 한 줄 기본값 `center` → `left`, 증거 있을 때만 `center` | 가운데 정렬은 한 글자만 줄어도 3~18px 민다 (실측) |
| 2 | **7-9 신설** | 폰트·크기·자간·행간·정렬을 따로 재고 총합을 낸다 | 한 숫자로는 "모양이 다른 것"과 "자리가 틀린 것"이 구분 안 된다 |
| 3 | 7-9 | 행간은 측정값만 보이고 **점수를 안 매긴다** | 원본 값을 그대로 쓰므로 재현 오차가 없다. 가짜 만점을 안 적는다 |
| 4 | 7-9 | 색 기준을 항목마다 다르게 | IoU 중앙값 47% 에 같은 잣대를 대면 폰트는 늘 빨갛다 |
| 5 | **7-6 통합** | `layout()`·`inkX()` 를 합성·판별·계측이 공유 | 따로 계산하면 화면 수치와 실제 결과가 갈린다 |
| 6 | 11장 | `초기화`(파일 삭제) → **`수정 초기화`**(문구만 되돌림) | 파일을 지우는 버튼에 '초기화'라는 이름이 붙어 있었다 |
| 7 | 11장 | 영역 표시를 꺼도 미리보기에서 블록을 고를 수 있다 | 보이는 것과 고를 수 있는 것은 다른 문제다 |
| 8 | 11장 | 원본/결과를 한 버튼 → **세그먼트 토글** | 이름이 바뀌는 버튼은 글자를 읽어야 현재 상태를 안다 |
| 9 | — | 다른 파일을 열 때 이전 합성 결과를 버린다 | 안 버리면 다운로드가 옛 그림을 내보낸다 |
| 10 | **배포** | `upload-pages-artifact` → `tar` + `upload-artifact` (`overwrite: true`) | 아티팩트가 두 개 등록돼 배포가 죽었고, 재실행으로도 안 풀렸다 (부록 H) |

### v1.3 → v1.4 (내 PC 폰트)

헤드라인 폰트는 산돌에서 산 유료 폰트였다. 번들할 수도, 흉내 내 만들 수도 없다.
설치된 폰트를 `local()` 로 불러 쓰는 길로 풀었다.

| # | 항목 | 변경 내용 | 근거 |
|---|------|-----------|------|
| 1 | **8-2 신설** | 유료 폰트는 사용자 PC에서 `local()` 로 불러 쓴다 | 저장소에 넣으면 재배포, 다시 그리면 복제본 |
| 2 | 8-2 | 등록한 로컬 폰트를 **자동판별 후보**에 넣는다 | 직접 선택은 전 블록 공통이라 헤드라인만 다른 이미지를 못 다룬다 |
| 3 | 7-7 | 다수결 단위를 `family` → `group` 으로 분리 | 로컬 폰트는 굵기마다 가족 이름(별칭)이 달라진다 |
| 4 | 8-2 | 열거(`queryLocalFonts`) 실패를 오류가 아니라 직접 입력 안내로 처리 | Firefox·Safari 는 API 자체가 없다 |
| 5 | — | `[hidden] { display: none !important }` 전역 규칙 | UA 규칙이 `display` 지정 클래스에 져서 숨긴 요소가 클릭을 가로챘다 (`.notes`·`.lf-panel`) |
| 6 | 13장 | 헤드라인 폰트 미해결 항목 종료 | 8-2 로 닫힘 |

### v1.2 → v1.3 (실사용 폰트 등록)

디자인팀 확인 결과 **SUIT · Pretendard · Noto Sans KR** 세 패밀리를 쓴다.
둘 다 OFL 이라 라이선스 제약 없이 등록했다.

| # | 항목 | 변경 내용 | 근거 |
|---|------|-----------|------|
| 1 | **8-1 후보군 재구성** | SUIT·Pretendard 6단계씩 추가, **자동판별을 세 패밀리로 한정** | 후보를 넓히면 정답 아닌 폰트가 IoU 만으로 이긴다 |
| 2 | 8-1 | 나머지 10종은 **직접 선택 전용**(`판별 제외` 표시), 고를 때 로드 | 자동판별을 좁히는 것과 선택 폭을 좁히는 것은 다른 문제 |
| 3 | **7-7 편향 기록** | IoU 가 굵기 일치를 과대평가 | Black Han Sans 58.6% vs SUIT 900 47.5% — 눈으로는 SUIT 이 가깝다 |
| 4 | **7-7 다수결 신설** | 확신 낮은 블록은 이미지의 지배적 패밀리를 따름 (4블록 이상 · 5%p 이내) | 같아 보이는 라벨 셋이 서로 다른 폰트로 갈렸다 |
| 5 | 13장 | 샘플 A 헤드라인 폰트는 세 패밀리 중 어느 것도 아님 | 본문은 재현되나 디스플레이 헤드라인은 여전히 다르다 |
| 6 | 9-2 | 최초 전송량 14.3MB → **13.5MB** | 판별 제외 폰트를 미리 받지 않는다 |

### v1.1 → v1.2 (비교 화면 정리 · 색 분리 후속 결함)

색 분리로 블록이 잘게 나뉘자 이웃 글자가 가까워지면서 합성 결함 두 건이 드러났다.
둘 다 색 분리 이전에도 있던 논리적 결함인데, 그때는 조건이 안 맞아 안 보였다.

| # | 항목 | 변경 내용 | 근거 |
|---|------|-----------|------|
| 1 | **7-5 배경 앵커 조건** | "마스크 밖" → **"마스크 밖 + 대표색에 가까움"** | 옆 블록의 `:` 를 배경으로 집어 들어 번지는 가로줄을 그렸다 |
| 2 | **7-3 비앵커 조각 배정** | 앞 구간에 무조건 → **가로로 가장 가까운 구간** | 물결표가 파란 라벨에 딸려가, 값을 고쳐도 물결표만 남았다 |
| 3 | **6-7 신설** | 같은 블록 재클릭으로 선택 해제, 원본/결과 전환 시 선택 해제 | 비교하려는데 편집 중인 블록 테두리가 글자를 가린다 |
| 4 | 6-7 | 내부 갱신은 선택 유지 | 되돌리기 후 결과가 없어지면서 선택까지 풀려 편집 맥락을 잃었다 |

### v1.0 → v1.1 (사용성 개선)

실사용 피드백을 반영했다. 불편의 뿌리는 두 가지였다 — 조작할 것과 볼 것이 뒤섞여
있었고, 결과를 보기까지 단계와 방해물이 많았다.

| # | 항목 | 변경 내용 | 근거 |
|---|------|-----------|------|
| 1 | **6장 재구성** | 조작은 상단 고정, 정보는 하단 고정 | 버튼이 상·하단에 흩어져 있었고, 정보가 이미지 높이에 따라 자리를 옮겼다 |
| 2 | **6-0 신설** | `[수정 요청]` 제거, **저장이 곧 반영** | 저장 후 화면이 그대로면 저장이 안 된 줄 안다. 기본 화면은 결과, 원본은 비교할 때만 |
| 3 | **6-6 신설** | `[영역 표시]` 토글 | 비교하려는데 블록 테두리가 덮여 글자를 못 본다. 끄면 선택한 것만, 선택까지 풀면 완전히 깨끗 |
| 4 | 폰트 컨트롤 | 라디오 + 드롭다운 → **드롭다운 하나** (`자동판별` + 후보 13종) | 두 컨트롤이 같은 것을 정하고 있었다 |
| 5 | **7-3 색 분리 신설** | 한 줄 안에서 색이 바뀌면 나눈다 | 파란 라벨 + 검은 값이 한 블록이라 색이 뭉개졌다 |
| 6 | 7-3 | 색 비교를 절대값 → **배경 대비 방향** | 얇은 획은 안티에일리어싱으로 옅게 잡혀, 절대값으로 보면 단색 문장이 찢어진다 |
| 7 | 7-3 조정 | 줄 그룹핑 간격 1.5 → **1.0** | 문단 0.86 vs 라벨 간 1.14 사이 |
| 8 | 13장 | "한 줄 두 색" 한계 해소, 편집 단위 분할을 대가로 명시 | 5번의 결과 |

### v0.11 → v1.0 (M6 완료)

다운로드와 배포 구성을 붙였다. 모든 마일스톤이 끝나 v1.0 으로 올린다.

| # | 항목 | 변경 내용 | 근거 |
|---|------|-----------|------|
| 1 | **9-2 신설** | 배포 구성 · 배포물 범위 · 전송량 명시 | Pages 하위 경로 서빙, 검증용 디렉터리 제외 |
| 2 | 다운로드 | `canvas.toBlob()` → `a[download]`, 파일명 `{원본}-edited.png` | 원본이 JPG여도 PNG로 내보낸다 |
| 3 | 13장 보강 | 최초 로딩 14.3MB 를 리스크로 명시 | `eng` 제거를 검토했으나 영문이 전부 깨져 불가 |
| 4 | 12장 | M6 완료, 전 마일스톤 종료 | 배포물 검증 9건 통과 |

### v0.10 → v0.11 (M5 완료)

M0에서 검증한 판별 로직을 앱에 옮겼다. 계획서가 요구하던 정규화 단계가 이미
불필요해졌다는 것이 가장 큰 변경이다.

| # | 항목 | 변경 내용 | 근거 |
|---|------|-----------|------|
| 1 | **7-7 정규화 재정리** | 크롭·리스케일·무게중심 정렬 → **7-6 피팅이 대신** | 피팅이 후보를 원본과 같은 상자에 같은 크기로 놓는다. 추가 정규화는 위치 정보를 버린다 |
| 2 | 7-7 | 판별과 합성이 **같은 렌더 경로**를 쓰도록 명시 | 다르게 놓으면 판별할 때 닮았던 폰트가 합성에서 달라진다 |
| 3 | **7-7 밀도 필터 미채택** | 잉크 밀도 1차 필터 폐기 | 밀도를 재려면 어차피 렌더해야 해 비용이 줄지 않는다. 13종 전부 계산해도 0.5초 |
| 4 | 7-7 | 낮은 확신 기준을 격차 4%p 미만으로 명시 | 실측 분포 기준 |
| 5 | 12장 | M5 완료 표시 | 검사 9건 통과 |

### v0.9 → v0.10 (M4.5 완료)

유형 B(단색 배경) 합성을 붙였다. 계획서의 "대표색 채우기"가 실물에서 통하지 않아
교체했고, 글자색 추출도 한 번 더 손봤다.

| # | 항목 | 변경 내용 | 근거 |
|---|------|-----------|------|
| 1 | **7-5 유형 B 제거 교체** | 대표색 채우기 → **줄 단위 좌우 배경 선형 보간** | 단색으로 보이는 배경도 위치에 따라 RGB가 10 남짓 변해, 한 색으로 칠하면 띠처럼 도드라진다 |
| 2 | 7-5 | **페더링 불필요** 확인 | 제거 마스크가 잉크보다 2px 크고 채우는 값도 그 배경에서 보간한 색이라 이음매가 없다 |
| 3 | **7-4 색 추출 재수정** | 거리 비례 가중 → **코어 픽셀만** (최대 거리의 65% 이상) | 경계 픽셀이 절반 넘는 무게를 받아 흰 글자가 `rgb(195,194,194)` 회색으로 잡혔다 → `rgb(249,249,249)` |
| 4 | 7-6 | 불투명도 승계는 유형 A에서만 | 유형 B는 불투명 배경 위라 알파를 낮추면 색이 흐려진다 |
| 5 | 13장 보강 | 한 줄에 색이 두 가지면 하나로 통일됨 | `이벤트 기간 : ~8/31(월)까지` 는 라벨 파랑 + 값 검정 |

### v0.8 → v0.9 (M4 완료)

M0에서 검증한 합성 경로를 앱에 넣었다. M0에 없던 두 가지(수정된 블록만 처리,
오버플로 규칙)가 붙었고, 그 과정에서 폰트 관련 보정 두 건이 나왔다.

| # | 항목 | 변경 내용 | 근거 |
|---|------|-----------|------|
| 1 | **7-6 블록 단위 크기 통일** | 줄별 피팅 → 블록 안에서 하나로 (중앙값 중 큰 쪽) | 글자 구성에 따라 잉크 높이가 달라, 원본에서 같은 크기였던 두 줄이 51.4/45.7px 로 갈렸다 |
| 2 | **7-6 자간 상한** | 폰트 크기의 ±15%로 절단 | 좁은 폰트가 폭을 채우려 자간이 +15.7%까지 커지고, 그 값이 짧아진 새 문구에 물려가 벌어졌다 |
| 3 | 7-8 보강 | 줄 수가 늘면 원래 줄 간격으로 아래에 이어 붙임 | 사용자가 줄바꿈을 추가하는 경우가 계획서에 없었다 |
| 4 | **6-5 신설** | 오버플로 조치 사유를 칩으로 노출, 재편집 시 결과 무효화 | 낡은 결과를 계속 보여주면 반영됐다고 착각한다 |
| 5 | 12장 | M4 완료 표시 | 픽셀 보존 검증 포함 5건 통과 |

> 1·2번은 모두 **폰트 불일치의 2차 효과**다. 근본 원인은 기본 폰트(Noto Sans KR)가
> 원본의 굵은 디스플레이체와 다르다는 것이고, **M5의 폰트 판별이 들어가면 완화된다.**
> 실측으로 확인했다 — 잘 맞는 폰트(Black Han Sans)에서는 두 줄 크기가 61.3px 로
> 일치하고 자간도 −1% 수준이다.

### v0.7 → v0.8 (M3 완료)

알고리즘 발견은 없었다. 편집 상태를 어떻게 모델링할지가 유일한 설계 판단이었다.

| # | 항목 | 변경 내용 | 근거 |
|---|------|-----------|------|
| 1 | **10장 상태 모델** | `saved` 플래그 → **`draft` / `editedText` 분리** | 불리언 플래그로는 "무엇이 확정본인가"가 모호하다. 값을 둘로 나누면 합성이 쓸 값이 명확해진다 |
| 2 | **6-4 신설** | 필터 3종 · 방향키 이동 · `Ctrl+Enter` 저장 | 블록 18개에서 목록 클릭만으로는 탐색이 답답하다 |
| 3 | 6장 보강 | 오버레이 ↔ 목록 양방향 선택, 필터 제외 블록은 흐리게 | 위치 감각을 잃지 않게 |
| 4 | 12장 | M3 완료 표시 | 상호작용 검사 23건 통과 |

### v0.6 → v0.7 (M2 완료)

검출과 OCR을 실제로 붙였다. 계획서의 검출 방식(투영 분리)이 복합 레이아웃에서
통하지 않아 교체했고, OCR 전처리는 실험으로 파라미터를 확정했다.

| # | 항목 | 변경 내용 | 근거 |
|---|------|-----------|------|
| 1 | **7-3 검출 방식 교체** | 투영 분리 → **RLSA + 연결요소**, 1차(알파)/2차(단색 패치) 2단계 | 샘플 B는 좌우 컬럼·배지·사진이 섞여 가로 투영이 전부 겹친다 |
| 2 | **7-2 알파 정규화 신설** | `255 - alpha` → `255 - alpha/opacity` | 반투명 글자가 회색으로 찍혀 대비 부족. 이것만으로 샘플 A가 정확해졌다 |
| 3 | 7-2 확대 배율 확정 | 줄 높이 목표 **64px** | 실험: 40px는 굵은 글자를 놓치고 96px는 오히려 나빠진다 |
| 4 | 7-2 PSM 고정 | 항상 **PSM 6** | 한 줄 블록에 PSM 7을 쓰면 더 나쁘고, 여러 줄에 쓰면 무너진다 |
| 5 | **7-1 판정 bbox 패딩** | 1차 3px, 2차 줄 높이의 35% | 타이트한 잉크 bbox는 배경 표본이 부족해 단색 배경도 C로 떨어진다 |
| 6 | 7-3 보강 | 잉크를 패치 **마스크 내부**로 제한 | 원형 배지는 bbox 모서리가 패치 밖이라 뒤 사진이 글자색에 섞인다 |
| 7 | 7-4 가중치 정리 | 잉크 0/1 → **글자 확신도 가중** (A: 알파, B: 배경 대비 거리) | 검은 배경 위 흰 글자가 회색으로 잡히던 문제 |
| 8 | 7-3 그룹핑 보강 | 글자색이 다르면 다른 블록 | 파란 `EVENT 02.`가 검은 인용문과 한 블록으로 묶였다 |
| 9 | **6-3 신설** | OCR 신뢰도 표시 + 70% 미만 경고 | 신뢰도가 성공(86~96%)/실패(0~79%)를 잘 가른다 |
| 10 | 9-1 확정 | Tesseract 자산 **자체 호스팅으로 확정** | jsdelivr·unpkg·tessdata.projectnaptha 모두 차단된 환경이 실재 |
| 11 | 13장 보강 | 회전 텍스트·사진에 걸친 도형 미검출 추가 | M2 관찰 |

### v0.5 → v0.6 (M1 완료)

M1은 뼈대라 알고리즘 발견은 없었다. UI 쪽에서 두 가지가 추가됐다.

| # | 항목 | 변경 내용 | 근거 |
|---|------|-----------|------|
| 1 | **6-1 신설** | 미리보기 배경 토글 (체커보드/흰색/검정) | 샘플 A 1행은 체커보드 위에서도 안 보이고 검정 배경에서만 드러난다 |
| 2 | **6-2 신설** | 업로드 직후 이미지 정보 표시 | 투명 비율·알파 유무를 먼저 보여주면 처리 가능성을 미리 가늠할 수 있다 |
| 3 | 11장 | `tools/screenshot.js` 추가 | 마일스톤 완료 판정을 화면과 수치로 함께 확인 |
| 4 | 12장 | M1 완료 표시 | 두 샘플 모두 정상 표시, 콘솔 오류 없음 |

### v0.4 → v0.5 (M0 스파이크 실행 결과 반영)

샘플 A로 "제거 → 재합성"을 실제로 돌렸다. **M0는 통과**했고, 그 과정에서 계획서
수정 사항 6건이 나왔다. 결과는 [부록 B](#부록-b-m0-스파이크-결과).

| # | 항목 | 변경 내용 | 근거 |
|---|------|-----------|------|
| 1 | **7-1 판정 규칙 재교체** | 알파 중앙값 → **α≤20 픽셀 비율 ≥ 15%** | 중앙값은 사실상 50% 문턱이라, 굵은 글자 + 타이트한 bbox에서 잉크가 과반이 되면 무너진다. 샘플 A 빨간 줄이 실제로 오판됐다 |
| 2 | **7-7 판별 단위 변경** | 이미지 단위 → **블록 단위** | 한 이미지 안에서도 줄마다 폰트가 다르다. 재현도 57.2% → 73.3% |
| 3 | **7-4 불투명도 추출 신설** | 잉크 알파 95백분위를 대표 불투명도로 | 샘플 A 1행은 α 최대 153인 반투명 글자. 색만 가져가면 진해진다 |
| 4 | 7-6 자간 추정식 수정 | 글자수 나눗셈 → **두 점 기울기 역산** | 자간이 마지막 글자 뒤에 붙는지가 구현마다 달라 글자수 기반이 틀린다 |
| 5 | 7-7 보강 | IoU를 블록 영역으로 한정 | 캔버스 전체로 재면 다른 블록 결과가 섞인다 |
| 6 | **4장 갱신** | 샘플 A는 2줄이 아니라 **3줄** | 흰색 반투명 줄이 흰 배경에서 안 보였다 |
| 7 | **8-1 신설** | OFL 6패밀리 13종 후보군 확정 | 외부 공개 결정에 따라 라이선스 제약 없는 구성 |
| 8 | 13·14장 | 폰트 라이선스 리스크 해소, 착수 전 확정사항 전부 종료 | 8-1 확정 + 외부 공개 결정 |

### v0.3 → v0.4 (기준 샘플 원본 실측 반영)

샘플 원본 2종을 저장소에 넣고 픽셀을 직접 측정했다. **v0.3의 배경 유형 판정
알고리즘이 틀렸다는 것이 이 과정에서 드러나 교체했다.**

| # | 항목 | 변경 내용 | 근거 |
|---|------|-----------|------|
| 1 | **7-1 알고리즘 교체** | bbox 바깥 링 샘플링 → **bbox 안쪽 알파 중앙값 + 최빈색** | 링 방식은 샘플 A·B 헤드라인을 모두 B로 오판. 대표색이 배경색이 아닌 **글자색**으로 잡혀 글자 자리에 단색 사각형을 칠하게 됨 |
| 2 | 7-1 신설 | "폐기된 링 방식" 실패 기록 절 추가 | 같은 실수를 반복하지 않기 위해 실패 원리를 남김 |
| 3 | 7-1 보강 | 애매하면 C로 잠그는 원칙 명문화 | 잘못 칠하는 것보다 "안 됩니다"가 안전 |
| 4 | **4장 전면 갱신** | 화면 판독 추정 → **실측값**으로 교체 | 아래 측정 결과 참고 |
| 5 | 7-4 보강 | 알파 가중 평균을 "권장"에서 **"필수"** 로 격상 | 샘플 B 각주는 불투명 픽셀 0%. 알파 가중 없으면 색 표본이 아예 없음 |
| 6 | 7-1 보강 | 대표색을 블록마다 따로 산출하도록 명시 | 파란 박스 배경이 위치에 따라 RGB 10 남짓 변함 |
| 7 | 13장 보강 | bbox 정확도 의존성, 유형 B 채우기 이음매 리스크 추가 | `추첨 10명` 배지가 bbox 오차로 C 판정 |
| 8 | 11장 | `tools/tier-probe.py` 추가 | 판정 로직 재현·검증용 |

**측정 결과 요약**

| | 샘플 A | 샘플 B |
|---|---|---|
| 포맷·크기 | RGBA PNG 1140×184 | RGBA PNG 1140×921 |
| 완전 투명 | 84.45% | 48.02% |
| 반투명 | 4.08% | 10.40% |
| 완전 불투명 | 11.47% | 41.58% |

**v0.3의 예상 중 틀린 것**: 샘플 B를 "투명 배경이 아닌 불투명 그래픽"으로 봤으나,
실제로는 **절반이 투명한 RGBA PNG**였고 상단 텍스트가 전부 유형 A였다. 유형 B가
필요한 것은 파란 박스와 배지뿐이다. 조건이 v0.3에서 가정한 것보다 좋다.

### v0.2 → v0.3 (실제 샘플 2종 검토 반영)

가장 큰 변경은 **"투명 PNG 전용"이라는 전제를 "배경 복원이 쉬운 경우 전용"으로
일반화**한 것이다. 샘플 B가 투명 배경이 아니어서 v0.2 기준으로는 한 글자도 처리할
수 없었는데, 단색 배경(유형 B)을 추가하니 대부분의 블록이 열렸다.

| # | 항목 | 변경 내용 | 근거 |
|---|------|-----------|------|
| 1 | **7-1 신설** | 배경 유형 A/B/C 판정을 파이프라인 최전방에 배치 | 샘플 B가 투명 배경이 아님. 되는 블록/안 되는 블록 구분이 선행되어야 함 |
| 2 | **7-5 확장** | 유형 B 제거 = 대표색 채우기 + 경계 페더링 | 구현 비용은 작고 커버리지 증가폭은 큼 |
| 3 | **6장 전면 수정** | 단일 컬럼 인풋 나열 → 이미지 오버레이 + 블록 목록 2단 | 샘플 B는 블록이 약 20개. 기존 UI로는 사용 불가 |
| 4 | **7-6 보강** | 수정된 블록만 합성, 나머지는 원본 픽셀 보존 | 전 블록 재렌더 시 손대지 않은 문구까지 폰트 근사로 열화 |
| 5 | 7-6 보강 | 원본 bbox 폭에서 자간(tracking) 역산 후 적용 | 두 샘플 헤드라인 모두 자간이 좁음. 자간 0이면 눈에 띄게 넓어짐 |
| 6 | 7-7 보강 | 잉크 밀도로 굵기 1차 필터 후 `폰트 × 굵기` IoU | 두 샘플 헤드라인이 ExtraBold 계열. 굵기 불일치는 바로 티가 남 |
| 7 | 7-3 보강 | 줄간격 기반 여러 줄 그룹핑 + textarea 입력 | 두 샘플 모두 "2줄 한 덩어리" 구조 |
| 8 | 7-3 보강 | 분리 임계값을 덩어리 높이에 비례 적용 | 샘플 B 내 글자 크기 편차 약 4배 |
| 9 | 7-2 보강 | 유형 B용 색거리 전처리 + 작은 글자 3~4배 업스케일 | 검은 원 위 흰 글자 등 정규화 필요. 각주 ~11px는 업스케일 없이 실패 |
| 10 | 7-6 보강 | 글리프 부재 감지 경고 | 샘플 B 헤드라인의 `♥`. 조용히 폴백 렌더되는 버그 |
| 11 | 7-8 보강 | bbox 확장 시 인접 블록 침범 검사 | 샘플 B는 블록이 빽빽해 캔버스보다 옆 블록에 먼저 부딪힘 |
| 12 | **4장 신설** | 기준 샘플 A/B 정의 및 역할 분담 | 마일스톤 판정 기준을 실물에 고정 |
| 13 | 마일스톤 | M4를 M4(유형 A) / M4.5(유형 B)로 분리 | 공유 경로를 A로 먼저 검증한 뒤 제거 분기만 추가 |
| 14 | 13장 보강 | JPG 입력, 저장 형식, 샘플 B 커버리지 불확실성 추가 | 샘플 B가 JPG일 가능성 |
| 15 | 절 번호 | 7-0~7-6 → 7-1~7-8 재정렬 | 배경 유형 판정을 최전방으로 옮기며 정리 |

### v0.1 → v0.2

| # | 항목 | 변경 내용 | 이유 |
|---|------|-----------|------|
| 1 | OCR 전처리 신설 | 알파 마스크 → 흑백 변환 단계 추가 (현 7-2) | 투명 픽셀이 검정으로 읽혀 흰 글자 OCR이 사실상 불가능 |
| 2 | 폰트 크기 | "역산" → 3회 피팅 루프 | 잉크 높이와 em 크기 비율이 폰트·언어별로 달라 단일 공식으로 안 맞음 |
| 3 | 오버플로 규칙 신설 | 자간→축소→확장→경고 4단계 (현 7-8) | 문구 길이 변화는 실사용 최빈 케이스인데 규칙 부재 |
| 4 | IoU | 크롭·리스케일·무게중심 정렬 추가 | 정규화 없는 IoU는 모양이 아니라 위치 오차를 측정 |
| 5 | 글자 제거 | bbox 사각형 → 글자 픽셀 dilate 마스크 | 사각형은 겹친 장식 요소까지 삭제. dilate는 잔상 방지 |
| 6 | 영역 검출 | OCR bbox 대신 잉크 bbox, 투영 기반 1차 분리 | OCR bbox 패딩이 크기 역산을 어긋나게 함 |
| 7 | 색 추출 | 알파 가중 평균 | 반투명 외곽 픽셀 동일 가중 시 색이 흐려짐 |
| 8 | Tesseract 자산 | 자체 호스팅 폴백 (현 9-1) | CDN 차단 환경 및 worker/wasm 경로 이슈 |
| 9 | 마일스톤 | M0(합성 스파이크) 최우선 신설 | OCR 오류는 수동수정으로 덮이지만 합성 품질은 못 덮음 |
| 10 | 데이터 모델 신설 | 상태 구조 명시 (현 10장) | 모듈 간 계약 고정으로 독립 구현 가능 |
| 11 | 리스크 | `document.fonts.ready` 타이밍 추가 | 조용히 폴백 폰트로 렌더되는 버그 |
| 12 | 착수 전 확정사항 신설 | 샘플·폰트·공개여부 3건 | 샘플 없이는 M0 검증 불가 |
| 13 | 구조 | `image-text-tool/` → `image-text-swap/` | 실제 저장소명과 불일치 |

---

## 부록 B. M0 스파이크 결과

**판정: 통과.** 합성 품질의 상한선이 충분히 높다. M1 이후를 진행할 근거가 확보됐다.

### 실행 방법

```bash
NODE_PATH=$(npm root -g) node spike/run.js     # 저장소 루트에서
```

헤드리스 Chromium에서 `spike/m0.html` 을 열어 Canvas 2D로 파이프라인을 돌리고,
결과 PNG 3장과 `metrics.json` 을 `spike/out/` 에 남긴다. 최종 목표 기술과 동일한
환경에서 검증하기 위해 파이썬이 아니라 브라우저에서 돌렸다.

### 조건

- 대상: `assets/sample-simple.png` (샘플 A)
- OCR 없이 문구 하드코딩. 파이프라인의 **합성 경로만** 검증
- 유형 A 경로(`alpha = 0`)만 사용. 유형 B는 M4.5

### 결과

| 블록 | 원문 | 유형 | 크기 | 자간 | 불투명도 | 판별 폰트 | 재현도 |
|---|------|------|------|------|----------|-----------|--------|
| 0 | 더 이상의 설명은 필요없습니다. | A (투명 59%) | 23.2px | +0.06px | 60% | Noto Sans KR 400 | **86.8%** |
| 1 | 2027 합격을 위한 행정법 | A (투명 37%) | 49.7px | −0.29px | 100% | Gothic A1 900 | 61.6% |
| 2 | 결국, 써니 뿐입니다. | A (투명 45%) | 61.3px | −0.64px | 100% | Black Han Sans 400 | 71.5% |

**블록 평균 재현도 73.3%** (원문을 그대로 다시 그린 뒤 원본 실루엣과 제자리 IoU).

### 확인된 것

- **제거가 깨끗하다.** dilate 마스크 방식으로 안티에일리어싱 잔상이 남지 않았다.
  유형 A에서 배경 복원이 불필요하다는 전제가 실물로 확인됐다
- **위치·크기·자간·색이 정확하다.** 원본과 재렌더를 겹친 차이 이미지에서 어긋남이
  **글자 형태에서만** 나타났다. 파이프라인 기하는 맞다는 뜻이다
- **남은 오차는 폰트 후보군의 문제다.** 후보에 실제 사용 폰트를 넣으면 재현도가
  더 올라간다. 알고리즘을 더 손볼 여지보다 후보군을 채우는 쪽의 효과가 크다
- **새 문구 교체가 자연스럽다.** 길이가 달라진 문구도 자간 승계만으로 깨지지 않았다

### M0가 찾아낸 결함 (전부 수정 후 재실행)

1. 7-1 알파 중앙값 규칙이 굵은 글자에서 오판 → 비율 규칙으로 교체
2. 이미지 단위 폰트 판별이 얇은 줄을 굵은 폰트로 뭉갬 → 블록 단위로 교체
3. 반투명 글자의 불투명도를 승계하지 않아 재렌더가 진해짐 → 불투명도 추출 추가
4. 샘플 A가 3줄인 것을 놓쳐 문구·블록 대응이 어긋남 → 4장 갱신

### 남은 확인 사항

- 유형 B(단색 배경 채우기) 경로는 **미검증**이다. M4.5에서 샘플 B로 확인한다
- OCR 정확도는 M0 범위 밖이다. M2에서 확인한다
- 오버플로 규칙(7-8)은 미구현이다. M0에서는 자간 승계만으로 충분했다

---

## 부록 C. M2 결과

**판정: 통과.** 샘플 A는 문구가 100% 정확하고, 샘플 B는 블록 수와 A/B/C 배지가
실물과 맞는다.

### 실행 방법

```bash
NODE_PATH=$(npm root -g) node tools/screenshot.js      # 업로드 → 검출 → OCR 전 과정
NODE_PATH=$(npm root -g) node tools/detect-probe.js    # 검출 결과 + 경계 시각화
```

### 성능

| 대상 | 블록 검출 | OCR (엔진 로딩 포함) |
|------|-----------|----------------------|
| 샘플 A (1140×184, 2블록) | 56ms | 0.6s |
| 샘플 B (1140×921, 18블록) | 540ms | 3.2s |

`kor.traineddata` fast판이 1.7MB로 작아 로딩이 걱정보다 훨씬 가벼웠다.

### 샘플 A — 3블록 중 3블록 정확 (2블록으로 그룹핑됨)

| 블록 | 유형 | 신뢰도 | 인식 결과 |
|------|------|--------|-----------|
| b0 | A | 93% | 더 이상의 설명은 필요없습니다. |
| b1 | A | 92% | 2027 합격을 위한 행정법 / 결국, 써니 뿐입니다. |

**오탈자 없음.** 알파 정규화를 넣기 전에는 `설명은`→`ABS`, `합격을`→`SAS` 로
깨졌다.

### 샘플 B — 18블록

| 상태 | 개수 | 내용 |
|------|------|------|
| 정확 또는 거의 정확 | 11 | 헤드라인·인용문·각주·파란 박스 전체·배지 2개 |
| 실패 (신뢰도 낮음) | 6 | 배달의민족 상품권 위 회전된 텍스트 |
| 오검출 | 1 | 사진 일부 (C로 잠금) |

주요 블록 발췌:

```
b0  [A] 96%  EVENT 02.
b2  [A] 91%  2027 핵집 너무 기대대요 / 핵심집약 기대평 댓글 EVENT
b3  [B] 90%  추첨 / 5명
b11 [A] 92%  *YES24 베스트셀러 > 국내도서 > 수험서 자격증 > 공무원 > 행정법 > …
b14 [B] 92%  2027 핵심집약 추첨 5명, 배달의민족 1만원 상품권 3명, / …
b16 [B] 90%  2027 BYU UPGRADE 부분 중 가장 마음에 드는 부분과 함께 / …
```

- **11px 각주(b11)가 거의 완벽하게 읽혔다.** 계획서는 이 크기를 "실패 기본 가정"으로
  뒀는데, 64px 확대가 예상보다 잘 통했다
- 남은 오탈자는 `기대돼요`→`기대대요`, `핵집`→`HS`, `핵심집약`→`BYU` 정도다.
  **인풋박스 수동수정이라는 안전망의 존재 이유**가 그대로 확인된 셈이다
- `♥` 는 인식되지 않았다. 예상된 결과이며 사용자가 직접 입력해야 한다

### M2가 찾아낸 결함 (전부 수정 후 재실행)

1. 투영 분리가 복합 레이아웃에서 무의미 → RLSA + 연결요소로 교체
2. 반투명 글자를 정규화 없이 흑백화해 대비 부족 → 알파 정규화 추가
3. 타이트한 잉크 bbox로 유형 판정 시 단색 배경이 C로 오판 → 판정용 패딩 추가
4. 원형 패치의 bbox 모서리가 글자색을 오염 → 잉크를 패치 마스크 내부로 제한
5. 유형 B에서 경계 픽셀 때문에 흰 글자가 회색으로 → 배경 대비 거리 가중
6. 색이 다른 줄이 한 블록으로 묶임 → 그룹핑에 색 조건 추가

### 남은 확인 사항

- 회전된 텍스트는 이번 버전에서 지원하지 않는다 (13장)
- 사진에 걸친 단색 도형 안의 글자는 놓칠 수 있다 (13장)
- 편집·저장은 M3, 합성은 M4/M4.5에서 붙는다

---

## 부록 D. M3 결과

**판정: 통과.** 상호작용 검사 23건 전부 통과.

### 실행 방법

```bash
NODE_PATH=$(npm root -g) node tools/edit-probe.js
```

화면이 뜨는지가 아니라 **"고치고 저장한 것이 합성 입력으로 남는가"** 를 확인한다.
실제 UI를 클릭·타이핑해 상태 전이를 검사한다.

### 검사 항목

| 구분 | 검사 |
|------|------|
| 선택 | 목록 클릭 · 오버레이 클릭 · 편집 패널 표시 |
| 편집·저장 | draft 반영 · 저장 전 미확정 · 저장 후 확정 · dirty 표시 · 하단 개수 |
| 필터 | `수정됨` 1개만 · `편집 가능` 이 잠긴 블록 2개 제외 (18 → 16) |
| 되돌리기 | 원문 복원 · dirty 해제 · 개수 초기화 |
| 잠긴 블록 | 선택은 가능 · 입력 비활성 · 안내 문구 |
| 키보드 | `↑` `↓` 이동 · `Enter` 포커스 · `Ctrl+Enter` 저장 |

### 설계 판단 — 값을 둘로 나눈 것

v0.7까지의 모델은 `editedText` 하나에 `saved` 불리언을 붙이는 방식이었다.
이러면 "지금 화면에 보이는 값"과 "합성이 써야 할 값"이 같은 필드에 섞여,
저장하지 않은 편집 중에 합성을 돌리면 확정하지도 않은 문구가 들어간다.

값을 둘로 나눴다.

```
originalText   OCR 원문. 되돌리기의 기준
draft          입력창의 현재 값
editedText     [저장]으로 확정된 값 — 합성(M4)이 쓰는 유일한 값
dirty          editedText !== originalText
```

`draft !== editedText` 이면 "저장하지 않은 변경"으로 표시한다. 이 구분 덕에
M4는 `dirty` 인 블록의 `editedText` 만 보면 되고, 편집 중 상태를 신경 쓸 필요가 없다.

### 남은 확인 사항

- `수정 요청` 버튼은 여전히 비활성이다. 합성은 M4/M4.5에서 붙는다
- 블록 수동 병합·분리는 이번 버전 비목표다. 검출이 틀린 블록은 문구 수정으로만 대응한다

---

## 부록 E. M4 결과

**판정: 통과.** 문구를 길게·짧게 바꿔도 깨지지 않고, 수정하지 않은 블록은
**픽셀 단위로 원본 그대로** 남는다.

### 실행 방법

```bash
NODE_PATH=$(npm root -g) node tools/compose-probe.js
```

### 검사 결과

| 사례 | 조치 | 소요 |
|------|------|------|
| 동일 길이 — `2028 합격을 위한 헌법 / 정답은, 써니 뿐입니다.` | 96%로 축소 | 32ms |
| 짧은 문구 — `2028 헌법 / 써니.` | 없음 | 24ms |
| 긴 문구 — `2028 공무원 시험 합격을 위한 행정법 완전정복 / …` | 70%로 축소 + 폭 확장 | 101ms |

세 사례 모두 **수정하지 않은 블록(b0) 6,424px 중 0px 차이**. 7-6의
"수정된 블록만 처리" 원칙이 실제로 지켜지는 것을 픽셀로 확인했다.

오버플로 규칙(7-8)은 1~3단계가 모두 실제로 발동했다. 4단계(경고 후 그대로)는
샘플 A에서는 재현되지 않았다 — 폭 확장으로 해결됐다.

### M4가 찾아낸 결함 (전부 수정 후 재실행)

1. **같은 블록의 두 줄이 다른 크기로 렌더됨** (51.4px / 45.7px). 줄별로 잉크 높이에
   맞추다 보니 글자 구성 차이가 크기 차이로 나타났다 → 블록 단위로 크기 통일
2. **자간이 과도하게 벌어짐**. 좁은 폰트가 원본 폭을 채우려 자간을 +15.7%까지
   키웠고, 그 값이 짧아진 새 문구에 그대로 물려갔다 → 자간 상한 ±15%

두 결함 모두 **폰트 불일치의 2차 효과**다. 잘 맞는 폰트에서는 나타나지 않는다.

| 폰트 | 1행 크기 | 2행 크기 | 2행 자간 |
|------|----------|----------|----------|
| Noto Sans KR 700 (M4 기본값) | 51.4px | 45.7px | **+15.7%** |
| Black Han Sans 400 (M0이 판별한 값) | 61.3px | 61.3px | **−1.0%** |

**M5의 폰트 판별이 근본 해결책**이라는 것이 수치로 확인된 셈이다. M4의 두 보정은
폰트가 안 맞을 때의 안전장치로 남는다.

### 남은 확인 사항

- 유형 B(단색 배경 채우기)는 **미구현**이다. 수정해도 건너뛰고 사유만 남긴다 → M4.5
- 폰트는 `Noto Sans KR 700` 고정이다. 직접 선택·자동판별은 M5
- PNG 다운로드는 M6. 지금은 원본/결과 토글까지만 된다

---

## 부록 F. M4.5 결과

**판정: 통과.** 파란 정보 박스와 검은 원 배지 모두 교체되고, **채운 자리에
이음매가 보이지 않는다.**

### 실행 방법

```bash
NODE_PATH=$(npm root -g) node tools/compose-probe.js
```

M4 사례 3건에 유형 B 사례가 더해져 검사 7건이 된다.

### 검사 결과

| 블록 | 배경 | 교체 |
|------|------|------|
| b12 (파란 정보 박스) | `rgb(193,220,255)` | `이벤트 기간 : ~8/31(월)까지` → `~12/31(수)까지` |
| b3 (검은 원 배지) | `rgb(35,34,33)` | `추첨 / 5명` → `추첨 / 9명` |

건너뛴 블록 없음. 합성 41ms.

### 계획서가 틀렸던 지점 — 대표색 채우기

13장에 "유형 B 채우기가 티 날 수 있다"고 적어둔 리스크가 실제로 발생했다.
파란 정보 박스는 **완전 단색이 아니라 위치에 따라 RGB가 10 남짓 변한다.**
대표색 하나로 칠하면 그 자리가 띠처럼 도드라진다.

**줄 단위로 좌우 배경을 찾아 선형 보간**하는 방식으로 바꿨다. 배경이 균일하면
대표색 채우기와 결과가 같고, 변하면 그 변화를 따라간다. 검은 원 배지처럼 정말
균일한 배경에서도 그대로 동작한다.

**페더링은 결국 넣지 않았다.** 제거 마스크가 잉크보다 2px 부풀려져 있어 경계
바깥은 이미 순수 배경이고, 채워 넣는 값도 그 배경에서 보간한 색이다. 섞을
경계 자체가 없다.

### M4.5가 찾아낸 결함

**흰 글자가 회색으로 잡혔다.** v0.7에서 넣은 "배경과의 거리에 비례한 가중치"는
방향은 맞았지만 충분하지 않았다. 경계 픽셀이 여전히 절반 넘는 무게를 받는다.

| | 검은 원 위 흰 글자 |
|---|---|
| 거리 비례 가중 (v0.7~v0.9) | `rgb(195,194,194)` |
| **코어 픽셀만** (v0.10) | `rgb(249,249,249)` |

배경과의 색거리가 그 블록 **최대 거리의 65% 이상**인 픽셀만 쓰도록 바꿨다.

### 새로 드러난 한계

**한 줄에 색이 두 가지면 하나로 통일된다.** `이벤트 기간 : ~8/31(월)까지` 는
라벨이 파랑, 값이 검정인데 블록당 색 모델이 하나(세로 그라데이션)라 지배적인
색으로 렌더된다. 줄 안에서 색으로 다시 쪼개는 것은 이번 버전 비목표다.

### 남은 확인 사항

- 폰트는 `Noto Sans KR 700` 고정이다 → M5
- PNG 다운로드와 배포는 M6

---

## 부록 G. M5 결과

**판정: 통과.** 자동판별이 고정 폰트보다 두 샘플 모두에서 더 닮은 결과를 낸다.
검사 9건 통과.

### 실행 방법

```bash
NODE_PATH=$(npm root -g) node tools/font-probe.js
```

### 성능

| 대상 | 판별 시간 |
|------|-----------|
| 샘플 A (2블록 × 13후보) | 0.2s |
| 샘플 B (18블록 × 13후보) | 0.5s |

밀도 1차 필터 없이 전수 비교해도 충분히 빠르다.

### 자동판별 vs 고정 폰트

원문을 후보 폰트로 다시 그려 원본 실루엣과 비교한 IoU 평균.

| | 자동판별 | 고정 (Noto Sans KR 700) | 차이 |
|---|---|---|---|
| 샘플 A | **72.8%** | 51.6% | +21.2%p |
| 샘플 B | **47.5%** | 41.6% | +5.9%p |

샘플 A의 개선폭이 큰 이유는 후보군에 충분히 닮은 폰트(Black Han Sans)가 있어서다.
샘플 B는 실제 사용된 브랜드 폰트가 후보에 없어 개선폭이 작다. **후보군이 실제
폰트에 가까울수록 효과가 커진다**는 13장의 서술이 수치로 확인됐다.

### 블록별 판별 (샘플 A)

| 블록 | 판별 | 유사도 | 2위와 격차 |
|------|------|--------|-----------|
| b0 (흰 반투명, 얇음) | Noto Sans KR 400 | 87% | 23.7%p |
| b1 (빨간 굵은 2줄) | Black Han Sans 400 | 59% | 1.0%p ⚠ |

M0의 판별 결과와 일치한다. b0는 격차가 커 확신이 높고, b1은 Gothic A1 900과
1%p 차이라 "후보 간 차이 작음"으로 표시된다.

### 샘플 B의 낮은 확신

18블록 중 **13블록이 격차 4%p 미만**이다. 후보군에 정답 폰트가 없으니 남은 후보들이
고만고만하게 닮은 것이고, 그 사실이 그대로 표시된다. 억지로 하나를 고르되 확신이
낮다는 것을 숨기지 않는 편이 낫다.

### 검증 도구에서 발견한 편향

처음에는 `detectFor` 가 상위 3개 순위만 돌려줬다. 그러면 검증 도구가 고정 폰트의
점수를 찾을 때 **고정 폰트가 3위 안에 든 블록만 비교에 들어가** 수치가 왜곡된다.
실제로 그 상태에서는 샘플 B가 58.8% vs 58.1%(차이 없음)로 나왔지만, 전체 순위를
돌려주도록 고치니 47.5% vs 41.6%로 제대로 된 차이가 드러났다.

### 남은 확인 사항

- PNG 다운로드와 GitHub Pages 배포는 M6
- 브랜드 전용 폰트를 후보에 추가하면 판별 정확도가 바로 올라간다 (8-1)

---

## 부록 H. M6 결과

**판정: 통과.** 배포물만 추려 Pages 하위 경로에서 서빙한 상태로 업로드 → 편집 →
합성 → 다운로드 전 과정이 동작한다. 검사 9건 통과.

### 실행 방법

```bash
NODE_PATH=$(npm root -g) node tools/deploy-probe.js
```

임시 디렉터리에 배포물만 복사해 `/image-text-swap/` 하위 경로로 띄운다.
**절대경로가 하나라도 섞이면 여기서 깨진다.**

### 검사 결과

| 항목 | 결과 |
|------|------|
| 하위 경로에서 검출·OCR·판별 | 블록 2 · OCR 2 · 판별 2 |
| 다운로드 파일명 | `sample-simple-edited.png` |
| 출력 PNG | 1140×184, colorType 6 (RGBA), 24KB |
| 404 응답 | 없음 |
| 콘솔 오류 | 없음 |

알파 채널이 보존되는 것을 PNG 헤더의 colorType 으로 확인했다.

### 전송량

배포물은 21.7MB지만 **최초 1회 전송은 14.3MB** 다. Tesseract 코어 세 변종 중
브라우저가 하나만 골라 가져가므로 나머지 7.8MB는 전송되지 않는다.

### `eng.traineddata` 제거 검토 — 기각

전송량의 27%(3.92MB)를 차지해 제거를 검토했다. `kor` 단독으로 두 샘플을 다시
인식해 비교한 결과 **기각**했다.

| | kor+eng | kor 단독 |
|---|---|---|
| 샘플 A | 2/2 동일 | 2/2 동일 |
| 샘플 B | — | **6/18 블록만 동일** |

`kor` 단독은 한글에서 오히려 나은 경우도 있었지만(`핵집`을 `HS`로 오인식하지 않음)
영문이 전부 무너졌다.

```
EVENT 02.  →  ㄷ66411 02.
YES24      →  ㄷ524
UPGRADE    →  1『<ㅅ^
UP!        →  1ㅁ!!
R 10명     →  ㅁ 10명
```

이벤트 이미지에 영문이 흔하므로 4MB를 아끼려다 품질을 잃는 거래다.

### 배포 — 완료

**주소: https://stjings.github.io/image-text-swap/**

처음 두 실행은 실패했다. 원인은 저장소에 Pages 가 켜져 있지 않은 것이었다.

```
##[error] Get Pages site failed. Please verify that the repository has
Pages enabled and configured to build using GitHub Actions
```

배포물 추리기까지는 정상이었다(`22M _site`). 저장소 설정을 사람이 바꾸는 대신
`configure-pages` 의 **`enablement: true`** 로 워크플로가 Pages 를 직접 켜게 했고,
세 번째 실행에서 전 단계 성공했다.

| 단계 | 결과 |
|------|------|
| 배포물 추리기 | success |
| configure-pages (enablement) | success |
| upload-pages-artifact | success |
| deploy-pages | success — `Reported success!` |

이후 이 브랜치에 푸시하면 자동 배포된다. 저장소 설정을 손으로 만질 일은 없다.

### 배포 실패 (v1.5) — 아티팩트가 두 개 등록됐다

9번째 실행이 죽었다. 코드가 아니라 업로드 단계였다.

```
Error: Multiple artifacts named "github-pages" were unexpectedly found
for this workflow run. Artifact count is 2.
```

15MB 를 올리느라 `Finalizing artifact upload` 이 **27초** 걸렸고, 그 사이 업로드가
재시도되면서 같은 이름의 아티팩트가 두 개 등록됐다. `deploy-pages` 는 이름이 하나일
것을 전제하므로 그대로 실패한다.

**재실행으로는 못 푼다.** 그 런에 이미 중복이 남아 있어 두 번째 시도도 같은 자리에서
죽었다(attempt 2). `upload-pages-artifact@v3` 는 이걸 막을 손잡이(`overwrite`)를
노출하지 않는다.

`tar` + `upload-artifact@v4` 로 직접 바꿨다. tar 명령은 그 액션이 내부에서 하는 것과
같고, 다른 점은 **`overwrite: true`** 하나다 — 같은 이름이 이미 있으면 지우고 올린다.
같이 넣은 것: 배포물의 `js/` 목록을 로그에 남긴다. "고쳤는데 화면이 그대로"일 때
코드 문제인지 배포 문제인지 여기서 갈린다.

10번째 실행 로그:

```
25M	_site
-rw-r--r-- 1 runner runner  5175 typo.js     ← 새 파일 들어갔다
Found 1 artifact(s)                          ← 2 → 1
Reported success!
```

**교훈: 배포 성공/실패를 사람 눈으로 확인해야 한다.** 푸시가 끝났다고 배포가 된 게
아니다. 이번엔 사용자가 "수정사항이 반영된 게 맞나"라고 물어서야 드러났다.

### 남은 확인 사항

- **실제 배포 URL 에서의 동작은 아직 확인하지 못했다.** 개발 환경의 프록시가
  `github.io` 를 막고 있어 여기서는 열 수 없다. 로컬에서 같은 조건(하위 경로 ·
  배포물만)으로는 통과했으므로 동작할 것으로 보지만, **브라우저로 한 번 확인이 필요하다**
- 최초 로딩 14.3MB 는 사내 사용에는 무리가 없지만 외부 공개 시 체감될 수 있다

---

## 부록 I. 내 PC 폰트 결과 (v1.4)

헤드라인 폰트는 산돌에서 산 유료 폰트였다. 번들도 재현도 할 수 없으므로,
설치된 폰트를 `local()` 로 불러 쓰는 길을 만들었다(8-2).

### 실행 방법

```bash
NODE_PATH=$(npm root -g) node tools/localfont-probe.js
```

이 컨테이너에는 산돌 폰트가 없어 시스템에 있는 **Liberation Sans** 로 검증했다.
등록 경로는 폰트가 무엇이든 같다.

### 검사 결과 — 17항목 전부 통과

| 항목 | 결과 |
|------|------|
| 설치되지 않은 이름은 거부 | OK — `NetworkError`, 후보 15종 유지 |
| 설치된 폰트 등록 | OK — 후보 15 → **16종** |
| 드롭다운 표기 | OK — `Liberation Sans · 내 PC`, `판별 제외` 표시 없음 |
| 가족 이름 충돌 | OK — `LF Liberation Sans` |
| **폰트 파일 네트워크 요청** | **OK — 0건** |
| 캔버스가 실제로 그 폰트로 그림 | OK — 377px vs 폴백 349.2px |
| 판별 순위표 참여 | OK — 16종 중 유사도 56.2% |
| 직접 선택 · 합성 반영 | OK |
| 새로고침 후 복구 | OK — `localStorage` 에서 재등록 |
| 제거 | OK — 후보 15종으로 복귀 |
| 열거 실패 시 안내 | OK — 직접 입력으로 유도 |

**요청 0건이 이 설계의 핵심 근거다.** 폰트 파일이 서버에서 오지도, 서버로 가지도
않는다. 재배포가 아니라는 것이 수치로 남는다.

### 열거 API 는 믿을 수 없다

`queryLocalFonts()` 는 헤드리스 크로미움에서 **API 는 존재하는데 목록을 하나도
돌려주지 않았다.** Firefox·Safari 에는 API 자체가 없다. 그래서 열거 실패를 오류로
띄우지 않고 "이름을 직접 적어 넣으세요"로 안내한다. 이름만 알면 등록에는 권한도
열거도 필요 없다.

### 덤으로 잡힌 결함 — `[hidden]` 이 안 먹던 요소들

패널이 `hidden` 인데도 클릭을 가로챘다. `[hidden]` 의 UA 규칙(`display:none`)은
우선순위가 낮아 `display` 를 지정한 클래스에 진다. `.lf-panel` 뿐 아니라 **`.notes`
(합성 안내)도 같은 상태였다** — 숨긴 채로 자리를 차지하고 있었다.
`[hidden] { display: none !important }` 로 한 번에 막았다.

`tools/deploy-probe.js` 가 이 수정 뒤에 실패했는데, 원인은 도구 쪽이었다.
`saveBlock()` 을 기다리지 않고 `runCompose()` 를 또 부르면 `composing` 가드에
걸려 즉시 돌아온다. 합성이 끝나기 전 상태를 검사하고 있었다. `await saveBlock()`
하나로 고쳤다.

### 남은 확인 사항

- **실제 산돌 폰트로는 검증하지 못했다.** 이 환경에 그 폰트가 없다. 등록 경로는
  폰트에 무관하지만, 산돌 폰트의 PostScript 이름이 무엇인지는 디자인팀 PC 에서
  확인해야 한다 (`설치된 폰트 목록에서 고르기` 로 찾는 것이 가장 빠르다)
- 로컬 폰트는 굵기마다 따로 등록해야 한다. Regular 만 등록하면 헤드라인의 Bold 는
  여전히 근사치로 간다. 판별 후보로서의 값을 제대로 보려면 쓰는 굵기를 다 넣는 편이 낫다
- 유료 폰트의 라이선스가 **웹폰트 임베드까지 허용한다면** `fonts/` 에 woff2 를 넣고
  `fonts.css` · `FontMatch.CANDIDATES` 에 등록하는 쪽이 더 간단하다. 다만 이 저장소는
  공개이므로 그 경우 배포를 비공개로 돌려야 한다

---

## 부록 J. 타이포 계측 · 정렬 결과 (v1.5)

### 실행 방법

```bash
NODE_PATH=$(npm root -g) node tools/typo-probe.js
```

### 정렬 — 문제와 확인

편집하면 글자가 옆으로 밀린다는 제보로 시작했다. 원인은 한 줄 블록의 기본 정렬이
`center` 였던 것이다. 실측:

| 블록 | 원문 | 가운데 정렬 | 왼쪽 정렬 |
|------|------|------------|-----------|
| b6 | (인식 실패) | −4.2px | **0.0px** |
| b9 | (인식 실패) | −2.7px | **0.0px** |
| b10 | (인식 실패) | −8.9px | **0.0px** |
| b11 | (인식 실패) | +18.0px | **0.0px** |
| b13 | `이벤트 기간 :` | +3.9px | **0.0px** |
| b14 | `~8/31(월)까지` | +5.6px | **0.0px** |

왼쪽 정렬로 판정된 9블록 전부 이동량 0.0px. 실제 교체(`~8/31` → `~9/31`)로도
잉크 시작 위치가 원본과 같음을 픽셀로 확인했다.

가운데로 남은 2블록(b0 `EVENT 02.`, b12 각주)은 이미지 한가운데 놓인 것들이라
가운데 정렬이 맞다. 이동량(2.3px · 5.5px)을 계측해 패널에 적는다.

### 계측 — 샘플 B 20블록

| 블록 | 총합 | 폰트 | 크기 | 자간 | 정렬 | 크기 | 자간 | 정렬 |
|------|-----|-----|-----|-----|-----|------|------|------|
| b13 `이벤트 기간 :` | 92% | 79 | 100 | 100 | 100 | 15.7px | −0.9% | 왼쪽 |
| b14 `~8/31(월)까지` | 87% | 67 | 100 | 100 | 100 | 15.7px | +0.7% | 왼쪽 |
| b17 `이벤트 참여 방법 :` | 89% | 71 | 100 | 100 | 100 | 15.8px | −0.6% | 왼쪽 |
| b3 `추첨 5명` | 91% | 78 | 100 | 100 | 100 | 16.1px | −3.1% | 가운데 |
| b11 (인식 실패) | 52% | 12 | 100 | **47** | 100 | 13.2px | +15.0% | 왼쪽 |
| b10 (인식 실패) | 62% | 26 | 100 | **66** | 100 | 25.9px | −15.0% | 왼쪽 |

**자간 점수가 인식 실패 블록을 정확히 골라낸다.** 자간이 상한(±15%)에 닿은 5블록은
전부 OCR 이 글자를 잘못 읽은 블록이다. 원문이 틀렸으니 그 폭에 맞을 리가 없다.
담당자는 이 항목만 봐도 "이 블록은 손대면 안 된다"를 안다.

### 행간 — 디자인팀 말이 맞았다

측정된 행간 배수: 1.35 · 1.36 · 1.37 · 1.43 · 1.43 · 1.73 · 1.90.
폰트 기본 줄 높이 대비 **+8% ~ +44%**. 디자인팀이 "자간 다음으로 행간을 만진다"고
한 것이 수치로 확인된다. 다만 우리는 원본 줄 간격을 그대로 재사용하므로 이 값이
합성 정확도에는 영향을 주지 않는다 — **보여 주는 것이 목적이다.**

### 검사 결과 — 19항목 전부 통과

| 항목 | 결과 |
|------|------|
| 총합 = 항목 가중 평균 | OK — 22블록 전부 일치 |
| 점수 0~1 범위 | OK |
| 왼쪽 정렬 블록 이동 0px | OK — 9블록 |
| 예전(전부 가운데) 기준으로는 밀림 | OK — 11/11블록 |
| 가운데 블록의 이동량을 계측·표시 | OK — 2.3px · 5.5px |
| 계측 크기 = 합성 크기 | OK — 23.092 / 23.092 |
| 계측 정렬 = 합성 정렬 | OK |
| 패널 5항목 · 총합 · 판정 문장 | OK |
| 폰트 변경 시 재계산 | OK — 86% → 69% |
| 수정 초기화 (파일 유지) | OK |

### 남은 확인 사항

- **크기 점수가 거의 항상 100%다.** 한 줄 블록은 그 줄에 맞춰 크기를 잡으므로
  당연하다. 여러 줄 블록에서만(b0 94%, b16 98%) 중앙값 통일의 오차가 드러난다.
  항목으로서의 변별력은 낮지만, 낮게 나오는 경우가 실제 문제인 것은 맞다
- 자간은 **원본 폭에 맞춘 값**이라 "디자이너가 준 자간"과 "폰트 불일치를 메운 자간"이
  섞여 있다. 정답 폰트를 등록하면(8-2) 두 값이 분리된다
- 정렬 추정 3번 규칙(이미지 중앙 ±2%)은 샘플 2종에서만 확인했다. 좌우 여백이
  비대칭인 디자인에서는 왼쪽으로 떨어질 수 있다

---

## 부록 K. 고해상도 검출 결함 (v1.6)

2280×326 헤드라인 이미지에서 검출이 무너졌다. **상수가 픽셀 고정이었다.**

시험용으로 같은 구조의 이미지를 만들어 재현했다(`assets/sample-headline.png` ·
3행 · 회색+검정 1행, 검정 2행, 파랑 3행). 사용자가 본 것과 같은 결함이 나왔다.

### 결함 1 — 한 줄이 조각났다

```
b3  "지금 공단기로"
b4  "넘어오면 전-직렬"
b5  "50만원 할인!"
```

`RLSA_X = 26` 은 낱말 사이를 메워 글자를 한 줄로 잇는 거리다. **낱말 사이는 글자
크기에 비례하는데 기준만 고정**이라, 80px 글자에서는 낱말이 안 이어졌다.

1차 평활로 글자 높이를 먼저 재고 그 높이의 0.5배로 다시 평활한다(`rlsaAuto`).
**거리는 늘리기만 한다** — 줄이면 작은 글자 이미지의 기존 동작이 바뀐다.
샘플 B 20블록이 bbox까지 그대로인 것으로 확인했다.

### 결함 2 — 크기가 다른 두 줄이 묶였다

```
b0  2줄 1382×165  ← 1행 검정 부분 + 2행
```

한 블록으로 묶으면 합성이 크기를 하나로 통일한다(7-6). 크기가 다른 줄을 묶으면
**그 통일이 두 줄을 다 망친다.** 사용자 화면에서도 `2줄 · 1300×180` 한 블록에
`Pretendard 800 · 79.9px` 하나가 적용돼 있었다.

기준 `ratio <= 1.3` 이 헐거웠다. 실측:

| 이미지 | 블록 | 줄 높이 | 비 |
|--------|------|---------|-----|
| 샘플 A | b1 | 49, 49 | 1.000 |
| 샘플 B | b1 | 17, 17 | 1.000 |
| 샘플 B | b3 · b7 | 16, 15 | 1.067 |
| 샘플 B | b16 | 15, 15, 14 | **1.071** ← 진짜 여러 줄의 최댓값 |
| 헤드라인 | (잘못 묶인 것) | 60, 73 | 1.217 |

`LINE_RATIO = 1.15` 로 조였다. 진짜 여러 줄 6블록은 전부 유지되고, 크기가 다른
헤드라인 두 줄은 갈린다.

### 고친 뒤

```
b0  1줄  868×61   "초시때 합격할 수 있지 않았을까?"
b1  1줄  686×60   (회색 — OCR 실패)
b2  1줄 1188×73   "여러분의 제대로 된 MES 응원합니다."
b3  1줄 1422×73   "지금 공단기로 넘어오면 전-직렬 50만원 할인!"
```

눈에 보이는 4줄 = 4블록. 파란 줄은 한 덩어리.

### PSM 7 재검토 — 다시 기각

회색 줄이 `"처음부 ⏎ 부터 공단기 들었다면` 으로 갈려 PSM 7(한 줄)을 다시 재 봤다.

| 이미지 | 한 줄 블록 | PSM 6 과 같음 | 다름 |
|--------|-----------|--------------|------|
| 헤드라인 | 4 | 3 | 1 (PSM 7 이 더 나쁨) |
| 샘플 A | 1 | 1 | 0 |
| 샘플 B | 14 | 14 | 0 |

유일하게 다른 블록에서 PSM 7 은 `"HEHE 공단기 들었다면` 을 냈다. **PSM 6 유지.**
v0.7 의 판단이 그대로 맞았다.

### 남는 한계 — 조용히 틀리는 OCR

회색 글자(`#8a8a8a`)를 `"처음부 그 / 부터 공단기 들었다면` 으로 잘못 읽었는데
**신뢰도가 89%** 로 높게 나왔다. 신뢰도 임계(70%)로는 못 거른다. 사용자 화면에서도
같은 오독이 같은 자리에서 났다.

다만 이 블록은 안전도가 `위험` 으로 뜬다 — 틀린 원문에 폭을 맞추려다 자간이 상한
(+15.0%)에 닿기 때문이다. **틀린 인식이 자간 지표로 드러나는 셈이다.** 우연이지만
실제로 작동한다. 그래도 원문 대조는 사용자 몫으로 남는다.

## 부록 L. 화면 용어 정리 (v1.6)

사용자 지적: "타이포 계측 원문 같이 알아듣기 힘든 용어", "교체해도 안전한지가
눈에 안 띈다", "맨 앞에 A는 뭐야".

| 전 | 후 | 이유 |
|----|----|------|
| `A` / `B` / `C` 배지 | 안전도 칩 (`안전`·`위험` 등) | 유형 문자는 내부 용어다 |
| `타이포 계측 · 원문 재현 기준` | `원본과 비교한 수치` | 담당자는 디자이너가 아니다 |
| 폰트 | 글꼴 | |
| 자간 | 글자 사이 (자간) | 디자인팀과 쓰는 말은 괄호로 남긴다 |
| 행간 | 줄 사이 (행간) | |
| 정렬 | 가로 위치 | |
| `인식 92%` | `글자 인식 92%` | 재현 유사도와 헷갈렸다 |
| 판정 문장 (맨 아래 회색 작은 글씨) | 편집창 맨 위 색 박스 | 제일 중요한 한 줄이 제일 안 보였다 |

### 배치를 바꾼 이유

수치 패널이 입력창 **위**에 있어서, 해상도 큰 이미지를 열면 입력창이 화면 아래로
밀렸다. 순서를 바꿨다:

```
안전도 칩 + 판정 문장   ← 제일 먼저 알아야 하는 것
원래 문구
[ 입력창 ]              ← 실제로 하는 일
저장 / 되돌리기
▸ 원본과 비교한 수치 96%  ← 근거. 접어 둔다
```

### 안전도 판정 기준

**고칠 수 없는 것을 먼저 본다.** 자리·크기·간격이 어긋나면 어떤 글꼴을 골라도
티가 난다.

| 판정 | 조건 |
|------|------|
| `편집 불가` | 유형 C |
| `위험` | 자간이 상한에 닿거나, 폭·위치·크기 점수가 기준 미달 |
| `문구 확인` | 위는 통과했지만 OCR 신뢰도 70% 미만 |
| `글꼴 다름` | 위는 통과했지만 실루엣 IoU 40% 미만 |
| `안전` | 전부 통과 |

샘플 B 20블록 실측: 안전 12 · 위험 4 · 문구 확인 2 · 글꼴 다름 2 · 편집 불가 2.
**`위험` 4블록은 전부 OCR 이 글자를 잘못 읽은 블록이다.**

---

## 부록 M. 넘칠 때의 처리 — 재작성 (v1.7)

사용자 제보: `합격`→`불합격`, `50`→`60` 으로 고쳤더니 **글자 굵기가 얇아지고
자간이 줄었다.**

### 원인 둘, 둘 다 7-8 의 설계 문제

실측:

| 편집 | 상자 폭 | 새 문구 폭 | 넘침 | 옆 여유 |
|------|--------|-----------|------|---------|
| `합격` → `불합격` | 868px | 924.8px | **56.8px (6.5%)** | 1221px |
| `50만원` → `60만원` | 1422px | 1424px | **2px (0.1%)** | 2276px |

**(1) 압축이 먼저였다.** 순서가 `자간 압축 → 폰트 축소 → 폭 확장` 이라, 오른쪽에
1221px 이 비어 있어도 먼저 자간을 조이고 폰트를 줄였다. 자간 압축(−3.23px/글자
× 16글자 ≈ −52px)으로도 모자라 폰트 축소까지 갔다. **낱말 사이가 사라지고 획이
얇아진 것이 이 두 단계의 결과다.**

블록 상자는 **원문 잉크가 차지한 크기**일 뿐 레이아웃 상자가 아니다. 옆이 비어
있으면 그냥 넘어가면 된다 — 디자이너가 손으로 고쳐도 그렇게 한다.

**(2) 압축량이 넘친 양과 무관했다.** 1px 이 넘치든 100px 이 넘치든 자간을 폰트
크기의 5%씩 깎았다. `50`→`60` 은 **2px** 넘쳤는데 글자마다 3.94px, 줄 전체로 83px
좁아졌다. 40배 과잉 반응이다.

### 새 순서

```
0. 들어가면 그대로            ← 옆 여유까지 포함해서 잰다
1. 필요한 만큼만 자간 압축     ← 고정 5% 가 아니라 기울기 역산
2. 폰트 축소 (하한 70%)
3. 그대로 두고 알린다
```

`usableWidth` 를 새로 뒀다. 정렬마다 쓸 수 있는 폭이 다르다:

| 정렬 | 쓸 수 있는 폭 |
|------|--------------|
| 왼쪽 | `right − box.x0` |
| 오른쪽 | `box.x1 − left` |
| 가운데 | `2 × min(cx − left, right − cx)` — **좁은 쪽이 한계다** |

좌우 경계 사이 거리를 그대로 쓰면 가운데 정렬 글자가 한쪽 이웃을 밟는다.

### 고친 뒤

| 편집 | 크기 | 자간 | 조치 |
|------|------|------|------|
| `합격` → `불합격` | 64.6px → **64.6px** | 0.78 → **0.78** | 넓어졌다고 알림 |
| `50만원` → `60만원` | 78.9px → **78.9px** | 0.72 → **0.72** | 없음 |
| 같은 문구 4배 길이 | 78.9px → 55.2px | 0.72 → −2.26 | 옆 영역 침범 경고 |

**짧게 늘어난 것은 안 건드리고, 정말 긴 것만 줄인다.**

### 검증 — compose-probe 6항목 신설

| 항목 | 결과 |
|------|------|
| 옆이 비어 있으면 크기를 안 줄인다 | OK |
| 옆이 비어 있으면 자간을 안 좁힌다 | OK |
| 조금 넘친 것에 큰 압축을 걸지 않는다 | OK — 2.0px 넘침에 압축 0 |
| 정말 길면 그때는 줄인다 | OK — 78.9 → 55.2 |
| 넓어지면 사용자에게 알린다 | OK |
| 가운데 정렬은 좁은 쪽 기준으로 잰다 | OK — 왼쪽 900 / 오른쪽 150 / 가운데 200 |

### 이 결함이 왜 안 잡혔나

**오버플로 경로에 검사가 하나도 없었다.** compose-probe 는 "긴 문구"를 넣어
보긴 했지만 *결과 이미지가 나오는지*만 봤고, 크기·자간이 얼마나 바뀌는지는
재지 않았다. 눈으로 봐도 "길어서 줄었나 보다" 로 넘어갈 만한 결과였다.

숫자를 안 재면 "그럴듯한 잘못"은 안 잡힌다. 이번에 그 6항목을 넣었다.

---

## 부록 N. "전부 조금씩 다르다" — 원인 분해 (v1.7)

사용자가 실제 이벤트 이미지로 `EVENT 02→03`, `너무→너무 너무`, `5명→10명`,
`~8/31→~9/6` 을 고쳐 보고 **"전부 원본과 조금씩 다르다"** 고 알려 왔다.

### 방법 — 문구 변화를 배제한다

문구를 **그대로 둔 채** 강제로 다시 그려 원본과 픽셀을 비교했다. 문구가 안
바뀌었으므로 남는 차이는 전부 '재현' 오차다.

| 블록 | 글꼴 (모양) | 폭 | 높이 | 시작 x | **잉크량** |
|------|------------|-----|------|--------|-----------|
| b0 `EVENT 02.` | SUIT 700 (68%) | 0px | −1px | 0px | **+2.6%** |
| b2 헤드라인 | SUIT 900 (38%) | 0px | −1px | 0px | **−30.0%** |
| b3 `추첨/5명` | SUIT 900 (78%) | 0px | +1px | 0px | **−2.6%** |
| b14 `~8/31(월)까지` | SUIT 400 (67%) | 0px | 0px | 0px | **−18.6%** |

### 결론 — 자리는 맞다. 글자 모양이 다르다.

**폭·시작 x 오차가 전 블록 0px 이다.** 크기도 ±1px. v1.5~v1.7 에서 고친
정렬·오버플로는 제 몫을 하고 있다.

차이는 전부 **잉크량**, 즉 획 굵기다. 그리고 그 크기가 모양 유사도와 정확히
같이 간다:

- 모양 78% → 잉크 −2.6% (거의 같다)
- 모양 38% → 잉크 **−30%** (원본보다 30% 얇다)

헤드라인은 산돌 유료 폰트다. 후보에 정답이 없으니 SUIT 900 이 최선인데, 그
폰트가 원본보다 훨씬 가늘다. **이건 근사 오차가 아니라 다른 글꼴이다.**

### 2차 효과 — 자간이 폰트 오차를 떠안는다

`fitTracking` 은 원문이 **원래 폭에 정확히 맞도록** 자간을 정한다. 폰트가
원본보다 좁으면 폭을 채우려고 자간을 벌린다. 헤드라인의 자간 +6.4% 는
디자이너가 준 값이 아니라 **폰트 불일치를 메운 값**이다.

그래서 눈에는 두 가지로 보인다 — 획이 얇고, 글자가 띄엄띄엄하다. 원인은 하나다.
정답 폰트를 등록하면(8-2) 둘 다 같이 사라진다.

### 3차 — OCR 오독이 그대로 굳는다

사용자 결과에서 `기대돼요♥` 가 `기대대요` 가 되고 하트가 사라졌다. 합성 문제가
아니다. **OCR 이 원문을 그렇게 읽었고, 사용자는 그 잘못된 원문을 고친 것이다.**
`♥` 는 한국어 학습 데이터에 없어 아예 인식되지 않는다.

원문 표시(`원래 문구`)가 이미 화면에 있지만, 사용자가 그것을 원본과 대조하지
않으면 오독이 결과물까지 간다. 이번 버전에서 구조적으로 막을 방법은 없다.

## 부록 O. 그림 위 글자의 경계 (v1.7 후속)

부록 N 을 조사하다 v1.7 이 만든 구멍을 찾았다.

`roomFor` 는 **다른 글자 블록**만 본다. 그림은 모른다. 검은 원 배지의 `5명` 을
고칠 때 옆에 글자가 없으니 **"948px 비어 있다"** 고 판단하는데, 실제로 글자가
놓일 수 있는 곳은 **원 안쪽 72px** 뿐이다.

v1.6 까지는 넘치면 무조건 줄였으니 이 구멍이 드러나지 않았다. v1.7 에서 "자리가
있으면 넓힌다"로 바꾸면서 **원 밖 사진 위로 글자가 삐져나갈 수 있게 됐다.**
`5명`→`10명` 은 30px 라 우연히 안전했지만 `100명` 이면 나간다.

`bgExtent` 를 새로 뒀다. 유형 B 블록의 세로 중앙 줄을 좌우로 훑어 **배경색이
이어지는 데까지**를 경계로 잡고, 이웃 경계와 좁은 쪽을 쓴다.

| | 이웃만 볼 때 | 바탕까지 볼 때 |
|---|---|---|
| 배지 `5명` 자리 | 948px | **72px** (원 지름과 일치) |

`100명` 을 넣으면 이제 원 안에 맞춰 자간을 좁힌다(−1.00px). 예전에는 그대로
넘쳐 나갔다.

**교훈: 완화가 곧 새 결함이다.** "억지로 줄이지 마라"를 고치면서 "그럼 어디까지
넓혀도 되나"의 답을 글자 블록만으로 구한 것이 잘못이었다. compose-probe 에
검사 1항목을 넣었다.

---

## 부록 P. OCR 인식률 — 무엇이 되고 무엇이 안 되는가 (v1.8)

사용자 제보 둘.
1. 포크·수저 아이콘을 글자로 읽어 `ff`·`has.` 같은 쓰레기를 뱉는다
2. `핵집` 을 `HS` 로 읽는다

정답을 손으로 적어 두고(11블록) 글자 단위 정확도(Levenshtein)로 재며 실험했다.

### 안 되는 것 — 전처리로는 더 못 올린다

| 조건 | 평균 정확도 |
|------|------------|
| **현재 (64px, 부드럽게)** | **97.3%** |
| 96px 확대 | 94.6% |
| 128px 확대 | 96.3% |
| 64px + 이진화 | 96.3% |
| 96px + 이진화 | 96.5% |

**현재 설정이 이미 최적이다.** 이진화는 `핵심집약 UPGRADE` 를 고치지만
(91→98%) 대신 다른 블록을 깨뜨려(b1 95→83%, b16 100→89%) 총합이 내려간다.

### 안 되는 것 — 언어를 바꾸는 것도, 신뢰도로 고르는 것도

`kor` 단독은 `핵집`·`핵심집약` 을 맞히지만 영문을 다 깨뜨린다:
`EVENT`→`ㄷ66411`, `UPGRADE`→`1『<ㅅ^`. 18블록 중 11블록에서 더 나빴다.

낱말 단위로 신뢰도가 높은 쪽을 고르는 것도 **안 된다.** 결정적 실측:

| | 낱말 | 신뢰도 |
|---|------|-------|
| kor+eng (오답) | `“HS` | **62** |
| kor 단독 (정답) | `핵` | **21** |

**신뢰도가 정답 쪽을 가리키지 않는다.** 이 하나로 "신뢰도 높은 쪽 채택" 전략이
전부 무너진다.

### 되는 것 — 두 신호를 함께 본다

한글이 섞인 블록의 라틴 낱말만 놓고 보면 이렇게 갈린다:

| 라틴 낱말 | 신뢰도 | 한글 낱말 중앙값 | kor 단독이 내놓는 것 | 정답? |
|-----------|-------|-----------------|---------------------|------|
| `“HS` | 62 | 93 | `“핵집` | ❌ 오답 |
| `BYU` | 23 | 93 | `핵심집약` | ❌ 오답 |
| `UP!` | 16 | 93 | `1ㅁ!!` | ✅ 정답 |
| `UPGRADE` | 92 | 93 | — | ✅ 정답 |
| `*YES24` | 85 | 93 | — | ✅ 정답 |
| `R` | 89 | 93 | — | ✅ 정답 |

신뢰도만으로는 못 가른다 — 정답인 `UP!`(16)이 오답인 `BYU`(23)보다 낮다.
**결정적인 두 번째 신호: kor 단독이 완성형 한글을 내놓는가.**
`“핵집`·`핵심집약` 은 가-힣이고, `1ㅁ!!` 의 `ㅁ` 은 낱자모라 걸러진다.

규칙:
1. 블록의 한글 비율 ≥ 30%
2. 라틴 낱말의 신뢰도가 한글 낱말 중앙값보다 20 이상 낮다
3. 같은 자리를 kor 단독으로 읽으면 **완성형 한글**이 나온다
→ 셋 다 맞으면 갈아끼운다

겹침 판정은 **한국어 낱말이 의심 구간 안에 드는가**로 본다. 의심 낱말 폭의 절반을
기준으로 삼으면 `“HS` 자리에서 `핵` 하나만 잡히고 `집` 이 빠진다 — 한국어 쪽은
한 음절씩 잘리는 일이 흔하기 때문이다.

**낱말을 다시 이어 붙이지 않는다.** 원문 문자열에서 그 낱말만 바꿔 끼운다.
이어 붙이면 `핵 1 권 만 지 겹 도록` 처럼 띄어쓰기가 다 깨진다.

| | 전 | 후 |
|---|---|---|
| b1 | `“HS 1권만…` | `“핵집 1권만…` |
| b18 | `2027 BYU UPGRADE…` | `2027 핵심집약 UPGRADE…` (100%) |
| **평균 정확도** | 97.3% | **98.3%** |
| OCR 시간 | 3.2s | 4.8s |

### 되는 것 — 그림을 글자로 읽은 블록 걸러내기

선물카드의 포크·수저 아이콘이 '배경 아닌 픽셀'이라 글자로 검출된다.
두 갈래로 거른다:

1. 신뢰도 < 55 이고 한글이 하나도 없고 12자 이하 → `ont`·`ff`·`has.`·`“Hiterpiz`
2. **획 굵기가 고르다** (가로 런 길이 90분위/중앙값 < 2.0) 이고 신뢰도 < 70
   → 포크 모양이 `소년` 으로 읽힌 것

2번이 핵심이다. 글자는 가는 세로획과 긴 가로획이 섞여 있어 이 비가 크고,
아이콘은 통짜라 작다. 실측:

| | 획 비 |
|---|------|
| 아이콘 (b4·b8) | **1.7 · 1.8** |
| 진짜 글자 20블록 | 2.3 ~ 4.3 |

쓰레기 5블록(b4·b5·b6·b8·b9)을 전부 잡고 진짜 글자는 하나도 안 잡는다.
`글자 아님` 으로 표시하고 '편집 가능' 목록에서 뺀다. **지우지는 않는다** —
판단이 틀렸을 때 사용자가 되돌릴 수 있어야 한다.

### 남는 오류

| 블록 | 정확도 | 내용 |
|------|--------|------|
| b2 | 93% | `기대돼요♥` → `기대대요`. `♥` 는 한국어 학습 데이터에 없어 인식 불가 |
| b19 | 92% | `댓글,`→`댓글.`, `UP!!`→`UP!` — 문장부호 |
| b10 | — | 기울어진 `10,000원` → `10,0008,` (13장의 회전 한계) |

문장부호와 특수문자는 이번 방식으로 못 고친다. 원문 대조는 사용자 몫으로 남는다.

---

## 부록 Q. 미리보기가 잘리던 문제 (v1.8)

세로가 긴 이미지(1140×921)를 열면 위아래가 화면 밖으로 나갔다.

```css
.viewport canvas { max-width: 100%; height: auto; }   /* 가로만 맞춘다 */
```

**가로만 맞추고 세로는 안 맞췄다.** 실측:

| 창 크기 | 스테이지 | 캔버스 | 잘림 |
|---------|---------|--------|------|
| 1600×1000 | 1190×819 | 1140×**921** | YES |
| 1400×820 | 990×639 | 950×**768** | YES |
| 820×1000 | 786×351 | 746×**603** | YES |

상자에 **원본 비율**을 걸고 가로·세로를 함께 100%로 제한했다. 비율은 이미지를
열 때 `app.js` 가 `aspect-ratio` 로 넣는다 — 캔버스의 실제 픽셀 크기와 무관하게
상자가 먼저 정해져야 `max-height: 100%` 가 풀린다(그러지 않으면 높이가 내용에
의존해 백분율이 안 풀리는 순환이 생긴다).

| 창 크기 | 캔버스 | 잘림 |
|---------|--------|------|
| 1600×1000 | 964×779 | no |
| 1400×820 | 741×599 | no |
| 820×1000 | 746×603 | no |

### 곁가지 — 미디어 쿼리가 진 이유

좁은 화면에서 미리보기가 납작해지길래 `.pane-main { min-height: 70vh }` 를
넣었는데 안 먹었다. **미디어 쿼리는 우선순위를 올려 주지 않는다.** 뒤에 나오는
`.pane { min-height: 0 }` 이 같은 우선순위로 이긴 것이다. `.pane.pane-main` 으로
한 단계 좁혀 해결했다. 820×1000 에서 스테이지 351px → **643px**.

edit-probe 에 세 창 크기 × (통째로 보이는가 · 비율이 안 망가지는가) 6항목을 넣었다.
